# Decision Log — Portfolixir product brief, 2026-08-12

Canonical memory for this brief run. Every decision, change and override is
recorded here as the conversation unfolds.

## Run context

- **Intent:** create. Trigger is decision gate B3.1 from
  `planning-artifacts/feedback-triage-2026-08-12.md` (ADR-0038 continuous
  feedback intake; the gate governs the next several sprints).
- **Stakes:** self-hosted single-operator tool in a public repository. Not
  investor-facing. Right-sized accordingly — no market sizing, no fabricated
  moat. The README is the one genuinely public artifact downstream of this
  brief, which is why positioning has to be honest rather than modest.
- **Inputs already on the table (not re-litigated in this run):** the owner's
  2026-08-12 feedback dump, the portfolio agent's requirements document
  (2026-08-11), and the triage's Rounds 2 and 3 decisions.
- **Working mode:** fast path. Everything decided in Rounds 2/3 is carried in as
  settled; `[ASSUMPTION]` tags mark anything inferred. Rationale: the owner
  already answered the four build-order questions in conversation, so a coaching
  re-interview would re-open closed decisions.

## Decisions carried in as settled (source: triage Rounds 2 and 3)

| # | Decision | Consequence for the brief |
|---|---|---|
| D1 | Two first-class audiences: the operator's LLM agent and the operator | "Who this serves" has two entries, not a primary and a secondary |
| D2 | Agent-first priority; the human view follows in the same or next batch, enforced as a close-out finding | Belongs in Scope as a working rule, not only in the process docs |
| D3 | API/MCP coverage becomes two-way: an agent-only capability may ship without a view, with the PR stating why | Amends the `AGENTS.md` coverage rule; brief states the principle |
| D4 | The blanket "no advanced reports" rule is replaced by a bounded list: derived metrics (a), comparison and decomposition (b), decision evaluation (c) are in; backtesting (d) needs its own gate | Scope section carries the four levels verbatim |
| D5 | Permanently out: broker connection, order creation/transmission, automated trading or payment, advice, raw news archive, external LLM calls from the app | Non-goals, stated as identity rather than as a backlog boundary |
| D6 | Every metric ships with its computation basis documented (series, window, reference) | Success criteria / quality bar |
| D7 | One operator per instance who attaches their own agent over MCP. No multi-tenancy, no third-party agent platform | Audience and README tone: "your holdings, your agent, your machine" |
| D8 | Local model: deterministic code first, model only as fallback for unstructured input, output always a proposal, first gated use is ADR-0021 PDF intake | Out of the brief's core scope; noted as a boundary |
| D9 | Derived values become a durable, continuously maintained, freshness-labelled layer (gate B3.2, ADR pending) | The enabling architecture claim behind both audiences |
| D10 | The README's opening definition is the public output of this brief | Named as a deliverable of the gate |

## Decisions taken during this run

- 2026-08-12 — Workspace created; `brief.md` skeleton on disk with
  `status: draft`. Fast path chosen (see above).
- 2026-08-12 — **Operator-side success criteria stay qualitative.** The owner
  states plainly that they have no measurements; the felt signal arrives through
  the chat (stale figures, visible tool-grinding, token burn). Recorded as such
  rather than converted into invented KPIs. Two cheap observable proxies are
  offered in the brief and explicitly tagged `[ASSUMPTION]` — proposed by the
  PM, not requested by the owner. Consequence: the agent-side criteria carry the
  measurable weight of this brief, which is honest, because that is where the
  operator experiences the system.
- 2026-08-12 — **Audience question resolved; the ambiguity was the PM's.** The
  question "is adoption by other self-hosters a goal or a side effect" was read
  as "would someone host this for you". Owner's answer settles it either way:
  open source, everyone hosts their own instance with their own data and their
  own model. Recorded as the deployment model, not a growth target — and it is
  the reason the README must make the value legible quickly. No positioning
  section beyond the honest comparison already in the brief.
- 2026-08-12 — **Structural framing added to the problem statement** (PM
  contribution, open to correction): the "nothing derived is ever stored" rule
  was read as "nothing derived may ever be kept", which is stricter than
  auditability requires and is the shared cause of both the operator's waiting
  and the agent's recomputation. This is the sentence the derived-value ADR has
  to answer.
- 2026-08-12 — `addendum.md` created for downstream depth: the `AGENTS.md`
  amendments (Project Goal, two-way coverage rule, maintenance lane), the
  derived-layer invariants, the local-model ordering, README story scope, the
  parked items with reasons, and the evidence-base note.

- 2026-08-12 — **Editorial passes run on `brief.md`** (structure and prose, per
  `doc_standards`). Applied: the count mismatches fixed (five drift cases were
  promised and three delivered; two working rules were announced as one
  sentence), Scope moved ahead of audience and positioning per pyramid order,
  the executive summary's third paragraph folded into one sentence, the
  operator-side justification de-duplicated against the problem statement, a
  gate header added naming what the brief decides and which artifacts change,
  the vague response-size figure replaced with the measured one, front-end
  jargon removed from the summary, and several hedges cut.
  **Two findings rejected, deliberately:**
  1. *"is thinner here than it should be" → "is largely missing here"* — this
     would be **false**. Closed trades are computed and exposed over the API and
     in the securities detail pane; TTWROR, IRR and MWR all exist. What is
     missing is a surface to read them on, and the text now says that instead.
  2. *"will stay ahead for a while" → "will stay ahead"* — dropping the bound
     asserts a permanent deficit against Portfolio Performance, which is the
     opposite of the plan.

- 2026-08-12 — **Editorial passes run on `addendum.md`.** The largest finding
  was a broken promise: the preamble named the PRD as the first downstream
  consumer and no section served the PRD author, who was sent to a 6,000-word
  triage document instead. A "For the PRD update" section now carries the
  requirement families, the four knowledge objects with their fields, the
  read-ergonomics constraint, and the audit-journal prerequisite. Second
  finding, nearly as costly: the derived-value invariants never named ADR-0032,
  although two of them *are* its mechanisms and the new ADR must supersede or
  amend it — an author starting from the five invariants alone would have
  rediscovered or contradicted an accepted ADR. ADR-0035 is now named as the
  adjacent precedent it must argue against.

  The same count-mismatch defect class found in the brief appeared twice more:
  "that ordering is the whole design" introduced five items of which only two
  were an ordering — the remaining three are the safety constraints (no
  judgment about money, proposal-not-write), which an ADR author could have
  discarded as commentary — and the preamble's list of downstream documents
  omitted the README although the file has a README section and the README
  author is a named reader.

  Also applied: sections reordered by downstream artifact with gate IDs in the
  headings, the metric-basis rule folded into the `AGENTS.md` section as a
  review gate with its venue named (the payload), the "reverse direction"
  mislabel on the coverage rule corrected to "symmetric", the maintenance lane
  anchored to Epic-Batch step 5, the evidence and privacy note moved into the
  preamble, and the blanket "nothing here is a new decision" disclaimer given
  the carve-out its own text contradicted.

  **One finding resolved against the reviewers' proposal:** both flagged that
  multi-tenancy is stated as identity in the brief and as parked here, and both
  wanted it added to the permanent non-goals. It is not added. That list holds
  what the owner forbade permanently — broker connection, orders, trading,
  advice. The owner declined multi-tenancy; they did not forbid it forever, and
  the wealth-vision parking-lot issue exists. The addendum now states the
  stronger-than-parked status explicitly and names where a future reversal
  would start, which resolves the inconsistency without inventing a permanent
  ban in the owner's name.

## Handling of this log's contents at finalize

- Carried into `brief.md`: D1–D7, D9, and both decisions of this run.
- Carried into `addendum.md`: D3 (amendment wording), D6 (as a review gate), D8
  (local-model ordering), D10 (README scope), plus the parked items.
- Process noise, deliberately set aside: the working-mode choice and the
  workspace mechanics.
