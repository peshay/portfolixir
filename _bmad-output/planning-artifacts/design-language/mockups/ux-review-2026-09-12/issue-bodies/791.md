## Scope

The impersonal-voice rule (owner rule 2026-07-23, review-blocking) and UX-DR11 ("every paragraph resolves to a tooltip, a data note, a basis line, or deletion") are broken by sixteen DE strings and their EN msgids. Found with `grep -nE "Tippe |Wähle |Lege |Passe |Klicke |\bdu\b|\bdein" priv/gettext/de/LC_MESSAGES/default.po`:

| DE string (start) | Where | Resolves to |
|---|---|---|
| "Tippe auf ein Segment oder fahre mit der Maus darüber für Details." | allocation sunburst | deletion (the centre carries the touched segment — sunburst story) |
| "Zeitraum %{range}. Passe den Reiter "Diagramm" an, um…" | securities Kurse tab | basis line "Zeitraum 1J · wie im Diagramm" |
| "Wähle einen Klassifizierungsbaum oder lege einen neuen an." | classifications index | deletion (the list and `+` are the sentence) |
| "Lege fest, wie dieses Wertpapier in jedem eigenen Baum klassifiziert wird…" | detail Klassifizierungen tab | ⓘ on the tab heading |
| "Tipp: Klicke Zeilen an, um mehrere auszuwählen, und ziehe sie dann…" | classification tree | deletion (select + toolbar are self-explanatory; drag is the accelerator) |
| "Lege die Standard-Buckets fest, die Positionen eines Depots…" | views page | basis line |
| "Für diese Sicht gibt es noch keinen Plan. Lege einen an oder übernimm…" | allocation | empty-state sentence: "Kein Plan für diese Ansicht." + the two actions as controls |
| "…Wähle oben eine Ansicht, um diese Seite einzugrenzen." | migration note | deletion of the second sentence |
| "Lege ein Depot mit Verrechnungskonto an, bevor du Transaktionen erfasst." | transactions empty state | "Buchungen brauchen ein Depot mit Verrechnungskonto." + the action |
| "Lege zuerst ein Depot mit Verrechnungskonto an…" (×5) | empty states | one sentence + one action each, no "zuerst … um zu" |
| "Wähle den Markt für" | security search dialog | field label "Markt" |
| the two-paragraph "So funktioniert's — in zwei Schritten" block | views page | ⓘ (views story #A7 owns the page; this sweep may land it if that story is not in the same batch) |

Also in scope: the securities detail form's three-line checkbox helper ("Für Anbieter, die Kurse vor einem Split…") becomes an ⓘ; the accent-bordered banner on the classification tree ("zieh es unten aus …") becomes a data note with a link (the tree-rows story may land it first).

## Where it is specified

- Review: [`ux-review-2026-09-12.md`](https://github.com/peshay/portfolixir/blob/claude/bmad-agent-ux-review-xsykdj/_bmad-output/planning-artifacts/ux-review-2026-09-12.md) → Part 1, D11.
- Spine: `EXPERIENCE.md` → Voice and Tone (impersonal voice; "prose is not the fallback"); UX-DR11.

## Acceptance

- The grep above returns zero matches outside quoted legal terms; EN msgids carry the same voice.
- No new `<p class="hint">` or `p.muted` paragraph is added; every replaced string is one of the four UX-DR11 outcomes.
- `localization_test.exs` green; `mix gettext.extract --merge` run.

Part of #356.
