# Decision Log — Portfolixir PRD (2026-06-12)

Canonical memory and audit trail for this PRD run. Every decision, change,
and override is recorded here as the conversation unfolds.

## Pre-run context (carried in)

- 2026-06-11 — project-context.md finalized (PR #345): verified stack, gates,
  domain invariants. Source: generate-project-context workflow.
- 2026-06-11 — Coverage policy revised: ratchet ~90%+ changed lines, no 100%
  hard goal (issue #314, panel decision).
- 2026-06-12 — Maintainer priority set (issue #321): (1) data completeness &
  consistency, (2) LLM-first MCP consumption with precomputed analytics,
  (3) UI/dashboard later. Dashboard v2 (#337) explicitly deprioritized.
- 2026-06-12 — Roadmap reorganized: correctness & invariants section first
  (#343, #344, #346–#348); LLM-first audit issue #349 created.

## Decisions

- 2026-06-12 — **Stakes calibration:** sequence, not either/or. (1) Solo-first,
  (2) launch-grade quality bar from the start (others must be able and want to
  use it; AI-agentic development means mechanical guards are load-bearing —
  the owner does not read code), (3) business option stays open, not forced.
  PRD depth: ~5-8 pages, cleanly phased, explicit scope changes.
- 2026-06-12 — **Scope tension surfaced:** the vision requires read-only
  broker/bank sync (comdirect first) and — far future — algotrading; both are
  currently forbidden by AGENTS.md hard rules. Resolution: PRD phases this
  explicitly; lifting the rule requires an ADR + AGENTS.md change, never
  silent drift. Research: aggregator free tiers collapsing (GoCardless, Salt
  Edge 2025); official comdirect REST API (read-only, OAuth2+PhotoTAN) is the
  viable route, FinTS fallback.
- 2026-06-12 — **"Playground" framing rejected** by maintainer: this is
  bleeding-edge AI-agentic engineering with production discipline, not a toy.
  Addendum corrected.
- 2026-06-12 — **New gate idea filed immediately** (working agreement rule 4):
  issue #350 — CI rejection of invisible/bidi Unicode characters
  (prompt-injection / Trojan Source defense for an agent-read repo).
- 2026-06-12 — **Working mode:** Fast path (consolidated gap questions, then
  full draft with [ASSUMPTION] tags).
- 2026-06-12 — **Sync provider scope confirmed:** comdirect (depot), bunq
  (cash accounts), bitcoin.de (BTC trades), watch-only wallet tracking
  (keys never touch the system).
- 2026-06-12 — **Tenancy model:** single user per instance; a second
  household portfolio within the same instance, served by filtered views
  (PP-style filters). Multi-user stays parked (#340).
- 2026-06-12 — **Product cornerstone confirmed:** Portfolixir never calls an
  LLM; LLMs call Portfolixir (read AND write via MCP). Intelligence stays
  external; the product is data + precomputed analytics.
- 2026-06-12 — **12-month success criteria confirmed as proposed:** (a) the
  manual spreadsheet + PP reconciliation fully retired, (b) the MCP agent
  answers cash allocation and "worth it vs. benchmark" without exports,
  (c) first credible early-retirement answer with real pension data.
- 2026-06-12 — **Reviewer gate (finalize):** rubric walker (verdict: strong,
  0 critical/1 high/4 medium/3 low) + adversarial review (2 critical/7 high/
  8 medium/4 low) + brain-dump reconciliation (8 gaps). All criticals/highs
  and reconciliation gaps fixed in one revision: new FR-28 (write audit
  journal) and FR-29 (backup + PP-roundtrip export); scope-gate annotations
  extended to FR-5(XML) and FR-12; FR-21 reworded to honest security claims;
  counter-metric reconciled with existing grandfathered baselines; web-UI
  no-auth made an explicit deployment assumption (OQ-8); FR-19 conditional on
  OQ-4; "maximum risk performance" restored (was distorted to
  "risk-adjusted"); "less research" + "make understandable" added to Vision;
  sync recorded as operator must-have (gate = when, not whether); glossary
  added; NFR-8 given p95 < 2 s; OQ-6/7/8 added. Review files preserved:
  review-rubric.md, review-adversarial.md, reconcile-braindump.md.
- 2026-06-12 — **Triage:** no phase-blockers among OQ-1..8; all deferred with
  owner + revisit condition (OQ-1 before first affected story; OQ-7 before
  FR-18; others bound to their phases).
- 2026-06-12 — **Polish:** folded into the post-review full rewrite
  (structure-then-prose order respected within the rewrite); editorial
  skills not separately dispatched — calibrated to solo stakes.
- 2026-06-12 — **Finalized:** prd.md status set to final.
- 2026-06-12 — **Market research digest** (web subagent): no comparable
  combines PP-import parity + MCP-first precomputed analytics + German
  pension modeling + what-if backtesting. Target-weight rebalancing is the
  weakest moat (PP has it). Maybe Finance's death validates investment focus
  over budgeting hybrid.
- 2026-06-12 — **Update run (post-architecture): FR-9 after-cost/after-tax
  dimension applied.** Source: PRD delta recorded in architecture.md
  (operator decision, 2026-06-12) — trading fees and German capital-gains
  taxes (Abgeltungsteuer, Vorabpauschale, Teilfreistellung) must be
  includable in the benchmark comparison; index scenarios "bought once and
  held" / "as a savings plan" named. Changes: FR-9 reworded; new OQ-9
  (tax-model depth, discovery shared with OQ-5, owner: maintainer, before first
  after-tax FR-9 story); OQ-5 cross-referenced; glossary +Abgeltungsteuer/
  Vorabpauschale/Teilfreistellung. No conflict with prior decisions: OQ-3
  phasing (fixed-rate first) preserved — index scenarios land with stage
  two. Reviewer gate skipped (solo stakes, narrow pre-decided delta).
  Status remains final.
- 2026-06-13 — **Privacy redaction pass (operator request, public repo):**
  persona anonymized to fictional "Alex"; personal financial history,
  amounts, household details, bank-account specifics, agent product name,
  and lifestyle references removed or generalized across prd.md,
  addendum.md, this log, reconcile-braindump.md, and review-adversarial.md.
  Product scope (provider integrations as planned features) intentionally
  retained. Branch history rewritten so the pre-redaction versions leave
  the public branch.

## 2026-07-25 — Validate run + UAT fix round

- **Reviewer gate (validate intent):** adversarial-general only; the rubric
  walker was deselected by the owner, so the run produced findings and a
  claimed-fix audit but no dimension verdicts. Grade derived from severity
  counts: Poor (2 critical, 10 high, 7 medium, 1 low). Artifacts:
  `review-adversarial-general.md`, `validation-report.{html,md}`. The prior
  `review-adversarial.md` was preserved and audited, not overwritten.
- **Claimed-fix audit result:** the 2026-06-12 log entry claiming "All
  criticals/highs ... fixed in one revision" was **not accurate** — 4 of 9
  genuinely resolved, 1 regressed (C-2 via the FR-29 rescope), 1 cosmetic
  (H-6), 3 untouched (H-2, H-5, H-7). Recorded here because this log is the
  canonical memory and the inaccuracy was load-bearing.
- **DECISION (owner, privacy override):** naming the operator (Andi) and
  naming concrete brokers/banks in connection with him is **acceptable** —
  the full name is public elsewhere. What stays out permanently: **concrete
  financial values** of any kind, and **anything about family or household**.
  This narrows the two reviewers' privacy findings rather than dismissing
  them. Applied: the "fictional persona name" label dropped (it was the
  dishonest part, not the content); "second household portfolio" / "family
  view" replaced with view-scoping language, which is also what ADR-0024
  requires; the addendum's "anonymized" labels corrected. The OD-2 file
  handling rule was kept as a fix — that is repo hygiene, not personal data.
- **DECISION (agent, flagged for review): PRD demoted rather than extended.**
  F-1 offered two options; `epics.md` had already amended its own authority
  claim and carries FR-30..FR-36, so the PRD is now
  `status: superseded-in-part` with an explicit pointer, instead of
  back-filling seven FRs into a document that would drift again.
- **DECISION (agent, flagged for review): FR-9 split into sub-ids FR-9a/b/c**
  rather than consuming new global ids — preserves append-never-renumber while
  making the requirement estimable. Method pinned in the FR: replay cash flows,
  compare end wealth/IRR; after-tax never applied to a time-weighted series.
- **DECISION (agent, flagged for review): scope gates annotated, not bundled.**
  FR-9/10/26/27 carry a section-level gate note against the AGENTS.md
  "no advanced reports" rule; OQ-1 was split into OQ-1a (XML, marked a
  **Phase-1 blocker** — the earlier "no phase-blockers" triage was wrong),
  OQ-1b (sync) and OQ-1c (advanced reports), with the FR-12 third recorded as
  resolved by ADR-0023.
- **Applied, from findings:** FR-29 rewritten to the shipped `pg_dump` scope
  with the safety claim removed and Metric 1 re-gated on FR-35; FR-14 split
  into 14a-d (delete, idempotency keys, dry-run, machine-readable errors);
  new NFR-9 mechanical scope backstop; FR-24/25 given a v1 assumption block
  tied to ADR-0031's parameter tables; FR-12 boundary restated in ADR-0023's
  words with the shipped/open halves marked; FR-4 and UJ-6 rewritten in
  bucket/view terms per ADR-0024; all three success metrics given
  instruments; OQ-8 made a Phase-3 precondition and FR-28 softened to
  actor-class attribution; NFR-2 corrected (reproducibility, not immutability);
  FR-13 given an analytics register and staleness reporting; FR-15/FR-16/NFR-8
  made falsifiable or explicitly aspirational; positioning downgraded from
  "research" to a maintainer scan with the cost reading of the pension gap
  added; production-readiness framing separated from engineering discipline;
  drift sign and unit pinned in the glossary.
- **New open questions:** OQ-10 (release/versioning/upgrade), OQ-11
  (historical quotes for never-held securities + chain data), OQ-12 (legal
  parameter-table ownership).
- **Mechanical:** frontmatter `updated` corrected to 2026-07-25; delivery unit
  corrected to epic batches (ADR-0026); phase-gate language reconciled; FR
  interleaving explained; glossary extended (view, bucket, portfolio, split,
  balance_adjustment naming trap).
- **Outside this PRD:** `_bmad-output/project-context.md` still required a
  `Model:`/`Thinking level:` commit footer that AGENTS.md and CLAUDE.md now
  forbid; corrected in the same commit.
- Status stays `superseded-in-part`; this was a fix round on validate
  findings, not a re-finalization.

## 2026-07-25 (later) — Owner decisions on the flagged items

- **DECISION (owner): PP XML full import (#333) has no priority.** PP JSON v1
  is the operator's actual data base and has proven the better path. #333
  moved out of Phase 1 into the parked list; FR-5 reworded to say JSON v1 is
  live and XML is parked. May return later.
- **Consequence:** OQ-1a is **dormant, not a blocker**. The scope gate itself
  stands unchanged — XML remains on the AGENTS.md forbidden list and would
  still need the ADR + amendment before any implementation — but nothing is
  blocked while XML is off the roadmap. This reverses the "Phase-1 blocker"
  marking made earlier today, which was correct for the roadmap as it stood
  and is now moot.
- **DECISION (owner): the three agent judgment calls are confirmed** —
  PRD demoted to `superseded-in-part`, `FR-DI-n` prefixing in the import PRD,
  and Feature B reframed to reconciliation. No changes.
- **DECISION (owner): remaining open questions accepted as structured** —
  OQ-1b, OQ-1c, OQ-8, OQ-10, OQ-11, OQ-12 keep their owners and revisit
  conditions. Generic illustrative amounts in the journeys stay generic.
- No pull request opened.

## 2026-07-25 (CI fix) — validation report kept as markdown only

- SonarCloud failed on PR #615 while every other check passed. Cause: the two
  generated `validation-report.html` files shared **309 identical lines**
  (~two thirds of each file) — the inline CSS block and page boilerplate —
  which trips the new-code duplication gate. `sonar.cpd.exclusions` covered
  only `test/**` and `mcp-server/test/**`.
- **Fix, two parts:** `sonar.exclusions=_bmad-output/**` added to
  `sonar-project.properties`, so agent-generated planning artifacts leave
  Sonar's analysis scope permanently; and the two HTML twins removed from the
  repo. The markdown twin is the canonical form per the validate workflow and
  carries identical content.
- The exclusion alone may need a one-time confirmation in the SonarCloud UI
  (Administration > Analysis Scope) to take effect under Automatic Analysis —
  the same caveat the properties file already documents for `cpd.exclusions`.
  Removing the HTML makes the build green without depending on that.
- Future BMad validate runs should write the HTML outside the repo, or the
  exclusion must be confirmed in the SonarCloud UI first.

## 2026-08-12 — Update run: the identity gate (B3.1) reaches the PRD

**Intent:** update. **Change signal:** the product brief of 2026-08-12
(`planning-artifacts/briefs/brief-portfolixir-2026-08-12/`), merged to `main`
as #663. The brief's own acceptance rule is the merge, so it is accepted, not
proposed — its frontmatter still says `proposed` and is stale as of that merge.
Its addendum carries a section titled "For the PRD update (gate B3.1)", which
is the handoff this run executes.

**Sources extracted, not ingested:** the brief and its addendum and decision
log, `feedback-triage-2026-08-12.md` (Buckets 3 and 4, Rounds 3 and 4), this
PRD, `epics.md`, and `project-context.md`.

### Owner decisions taken at the start of this run

- **DECISION (owner): the PRD carries the requirement *families*; `epics.md`
  carries the numbered FR-37 and beyond.** This resolves the conflict recorded
  below in favour of the registry rule this PRD already states. The PRD says
  what the product must now be able to do and why; the registry says which
  requirement is committed to which issue. Neither document claims authority
  twice.
- **DECISION (owner): this run delivers all of gate B3.1** — the PRD and its
  addendum, the `AGENTS.md` amendment, and the README opening definition. The
  gate closes in one branch rather than leaking across three.

### Conflicts surfaced before applying

1. **FR-1 contradicts the accepted brief.** FR-1 reads "holdings, balances, and
   performance are projections, **never stored facts**". The durable derived
   layer (brief D9, gate B3.2) stores exactly such values. The brief names this
   reading — "nothing derived is ever stored" implemented as "nothing derived
   may ever be kept" — as the shared cause of both the operator's waiting and
   the agent's recomputation. FR-1 is therefore reworded around
   *rebuildability* rather than *absence*, which is what ADR-0004 actually
   protects. **Risk-tier attention:** this is projection semantics, so it gets
   its own commit and an explicit callout in the reviewer briefing.
2. **Registry conflict.** §5 of this PRD says "New requirements go to
   `epics.md`, not here"; the brief addendum says new requirements start at
   FR-37 as part of the PRD update. Resolved by the owner decision above.
3. **OQ-1c resolves only partially, and the remainder is a real gap.** The
   ladder (a)-(c) *is* the advanced-reports amendment OQ-1c was waiting for, so
   the section C scope gate lifts for FR-9 and FR-10. It does **not** lift for
   FR-27: blind-follow backtesting is literally ladder level (d), which the
   owner put out of scope for now. And FR-26 (retirement projection) appears
   nowhere in the ladder — it is neither released nor gated by it. Recorded as
   an open question rather than decided by the agent.

### Decisions taken during this run

- 2026-08-12 — **FR-1 reworded around rebuildability.** The four bound
  properties (rebuildable, versioned, freshness-explicit, never authoritative
  for a write) are what ADR-0004 actually protects; "never stored" was a
  stronger rule than the guarantee needed, and the brief names that
  over-reading as the shared cause of both the operator's waiting and the
  agent's recomputation. Given its own commit per the risk-tier rule
  (ADR-0036), with a silent-staleness counter-metric added to §7 because the
  change introduces a failure mode the old rule made impossible by
  construction.
- 2026-08-12 — **The section C scope gate is resolved unevenly, and the PRD
  says so rather than smoothing it.** FR-9 and FR-10 released; FR-27 re-gated
  under ladder level (d); FR-26 recorded as outside the ladder entirely
  (**new OQ-13**). The tempting move was to declare the gate lifted and let
  the three cases sort themselves out later; a gate that lifts partially is
  precisely the kind a reader mistakes for lifted entirely, which is why
  NFR-9 now names its guarded set explicitly.
- 2026-08-12 — **New OQ-14:** whether the metric computation basis must
  surface in the UI or may reach the human through the ⓘ tooltip pattern. The
  payload half is settled and binding; the addendum author flagged the UI half
  as open and it is not the PM's to close.
- 2026-08-12 — **Story Workflow keeps nine steps.** The metric-basis check was
  first drafted as a tenth numbered step; `workflow_docs_test.exs` asserts the
  nine steps verbatim across `AGENTS.md`, `README.md`, `CONTRIBUTING.md` and
  `docs/development/story-workflow.md`, so a tenth step would either fail the
  meta-test or, worse, pass it while splitting the canonical numbering across
  four documents. Both clarifications now ride the existing steps. Caught by
  reading the meta-test before committing, not by the test failing.
- 2026-08-12 — **Advanced *classifications* stay out of scope.** The retired
  hard rule covered reports *and* classifications; the ladder covers analytics
  only. The `AGENTS.md` amendment says so explicitly rather than letting the
  replacement quietly widen the opening beyond what the owner confirmed in
  Round 3.
- 2026-08-12 — **Multi-tenancy recorded as declined-for-this-horizon, not as a
  permanent non-goal**, carrying forward the brief's resolution against its own
  reviewers. The permanent list holds what the owner forbade; they declined
  multi-tenancy rather than forbidding it.
- 2026-08-12 — **The Positioning section's differentiator list is flagged as
  partly stale.** It names what-if/backtest scenarios as one of four
  differentiators, and that is now ladder level (d) and gated. Annotated rather
  than rewritten: the list is a dated landscape scan, and editing its content
  silently would destroy the record of what was believed when.
- 2026-08-12 — **Two pre-existing inconsistencies recorded in `addendum.md`,
  not fixed:** `project-context.md` claims LiveView 0.20.x while 1.2.8 is
  installed, and `epics.md`'s Requirements Inventory still carries the
  pre-2026-07-25 copy of FR-1..FR-29. Scope lock applies; both are follow-up
  notes.
- 2026-08-12 — **README tone tension resolved toward the brief.**
  `project-context.md`'s microcopy rule says write doc text impersonally; the
  brief prescribes "your holdings, your agent, your machine". The later,
  README-specific instruction won for that phrase; the rest of the section
  stays impersonal. Recorded for owner confirmation rather than treated as
  settled.
- 2026-08-12 — **Gates run:** full `mix test` green (1755 tests, 6 properties,
  0 failures), `pre-commit run --all-files` green, meta-tests `docs_test`,
  `workflow_docs_test` and `ci_test` green. Commit authorship set to the
  accountable human before the first commit — the environment had defaulted to
  a bot identity, which the `commit-msg` hook would have rejected.

### Reviewer gate, 2026-08-12 — four reviewers, findings and disposition

Reviewers: PRD quality rubric, correctness hunter (claims vs. repository),
consistency hunter (internal contradictions and peer documents), scope and
privacy auditor. Full reviews sit beside this file as `review-*-2026-08-12.md`.
Clean across all four: privacy (zero hits — the brief and triage material was
correctly genericized), commit authorship, ADR citations, issue numbers, and
FR-37..FR-48 numbering with no collisions.

**The most expensive finding was mine, and it was believed rather than
checked.** The brief's addendum states the audit-journal rollout is incomplete
and that #677 blocks the knowledge objects. I carried it into both the PRD and
`epics.md` as a binding dependency without verifying it. It is false on every
count — ADR-0017 has a "Rollout complete" section, `@grandfathered` is the
empty set on a shrink-only list, all four supposedly unjournaled contexts call
`Journal.record/3`, MCP write tools ship, and FX is deliberately *never*
journaled rather than armed. The claim is a copy of the superseded 2026-06-18
reconciliation. Corrected in both documents with the evidence recorded.
**Consequence beyond this PR: issue #677 rests on the same premise and needs
rescoping or closing** — it was filed today against work that is done.

**Owner decision taken during the gate.** The brief's permanent non-goal "no
broker connection" contradicted Phase 3's read-only sync, an operator-stated
must-have this PRD has carried since 2026-06-12, and NFR-9 wants that list
mechanically enforceable — so the ambiguity was not survivable. Owner chose to
**precise the non-goal to an order-placing connection**: no placing, modifying
or transmitting orders and no moving money, while read-only acquisition stays
permitted in principle and gated in practice. Recorded as a precision, not a
relaxation: nothing forbidden before the sentence is permitted after it.

Other confirmed findings, all fixed on the branch:

- **NFR-9 asserted meta-tests that do not exist** — a false claim that predates
  this update and that this update extended to new gates. Of the three named
  backstops only `mcp_dependency_allowlist_test` exists. Changed from "are
  backed" to "must be backed", with the gap stated in both the PRD and
  `epics.md`. An unbuilt requirement described in the present tense is worse
  than no requirement: it stops anyone from building it.
- **Registry precedence pointed at a document known to be stale.** "Where the
  two disagree, `epics.md` wins" was issued two sections away from this PRD's
  own record that the epics inventory carries a superseded copy of
  FR-1..FR-29. Split into: `epics.md` wins on commitment, the PRD wins on
  intent and requirement wording, and the more recent dated revision wins on
  FR text.
- **The self-hoster was dormant and not dormant in the same document** — §1
  still deferred the persona while §2 promoted it to the deployment model. The
  one question the gate existed to settle, answered twice. §1 corrected.
- **The ladder retired "advanced *classifications*" in the PRD while
  `AGENTS.md` kept them out.** The partial lift misread as total — the exact
  failure NFR-9 warns about, committed in the same batch that warns about it.
  Both documents now state the analytics/classification split, and FR-42's
  exposure decomposition carries an explicit boundary against stored
  partial-weight assignments.
- **Level (c) was indistinguishable from the forbidden level (d).** A
  distinguishing test now sits in §4: (c) scores what was recorded before its
  outcome was known; (d) manufactures a counterfactual. FR-48 was the case
  that exposed it.
- **Success Metric 2's detector was invalidated by FR-37**, which it now sits
  beside — "no bulk `securities_list` pull" would have been failed by the very
  read-ergonomics family built to satisfy the metric. Retargeted at response
  volume rather than endpoint name.
- **The derived layer had a counter-metric but no success instrument.** New
  Metric 9: drop-and-rebuild reproduces the served figures with exact `Decimal`
  equality, and the rebuild's wall-clock is recorded.
- **`CONTRIBUTING.md` still carried the retired rule and the one-way coverage
  rule.** Updated. Its coverage sentence is the only string satisfying an
  assertion in `workflow_docs_test.exs`, so the sentence was kept verbatim and
  the reverse direction added beside it — changing it would have required
  editing the meta-test in the same commit.
- **The claim that the nine Story Workflow steps are "asserted verbatim across
  four documents" was wrong**, and I wrote it. The test asserts against the
  *concatenation*, so one document carrying a step satisfies it for all four —
  meaning a renumbering here would pass CI while silently disagreeing with
  `docs/development/story-workflow.md`. Reworded to say exactly that.
- **Two gated boundaries had no hard rule after the amendment** (B3.3 data
  acquisition, B3.7 push delivery — the second a security surface). Both added.
- **README and docs landing overclaimed.** Unbuilt knowledge objects were
  described as existing, "each stating the age of its inputs" promised a
  payload half this batch's own addendum records as absent, and both public
  entry points had dropped every maturity caveat while the PRD in the same
  batch promoted third-party self-hosting. Claims moved to the honest tense and
  the trusted-network / no-upgrade-guarantee caveat restored to both.
- **The hard-gate enumeration was stale in three places** — it named three
  gates and had been overtaken twice. Replaced by one table.
- **Journeys deliberately not written.** The gate opens journeys worth telling
  (a stale thesis, an unflattering calibration report, an event on an unowned
  security). They are captured from the operator, not authored by the PM;
  §3 now says so rather than leaving the omission to look like an oversight.

One reviewer claim was **rejected on verification**: the scope auditor read
`epics.md` as saying MCP writes are blocked, and concluded the README's
API/MCP sentence overclaims on that basis. MCP write tools ship today — the
correctness hunter's independent check and my own agree. The README sentence
was still softened, for the different and real reason that "everything it
knows" oversells coverage.
