# Decision Log — Portfolixir PRD (FX-Correct Settlement & Risk/Concentration)

Canonical memory and audit trail. Every decision, change, and override is recorded here.

## 2026-06-14 — Discovery

- **Intent:** Create. Fresh workspace bound at `prds/prd-portfolixir-2026-06-14/`. Prior PRD `prd-portfolixir-2026-06-12` is `status: final` — not resumed.
- **PRD scope (from John↔Andi prioritisation + Jordan answers):** First validating slice = **FR-B Phase 1** + **FR-D Slice A**. FR-A and FR-C are sequenced later, out of this PRD.
- **FR-B Phase 1 scope locked:** (a) transaction bookable in security currency with stored settlement-FX; (b) FX-corrected per-position P&L (kills phantom P&L, ref gap #343); (c) negative-balance accounts classifiable as credit / out of "liquidity". Hard constraint: at least one FX rate must exist before (b) ships. FX auto-sync (scheduler) is **fast-follow, NOT in MVP**.
- **FR-D Slice A scope locked:** single-call risk endpoint — Top-N single-name concentration + HHI + asset-class cap violations. Drawdown/volatility explicitly **NOT in MVP** (needs quote-history time-series math; deferred to Slice B).
- **Consumer:** AI agent "Jordan" uses Portfolixir exclusively via MCP as sole source-of-truth. Every new API endpoint requires a matching MCP tool (ADR-0002).
- **FR-A note (out of scope here):** paid providers (EODHD/FMP) rejected by Andi (no spend). Free Yahoo-scrape path only → fragile, declared-only forecasts; sequenced last.

### CM1 — Account liquidity role (Jordan, 12:58)

- New per-account field `liquidity_role` ∈ {`free_cash`, `credit_line`, `reserve`}. **Type beats sign.**
  - `free_cash`: counts as deployable liquidity only when balance ≥ 0.
  - `credit_line` (Lombard/Dispo/Margin): NEVER counts as free liquidity even when positive; negative = drawn credit → liability, reduces net worth; unused headroom is not liquidity (optionally shown separately as "available leverage", out of the cash quote).
  - `reserve` (earmarked, e.g. BTC autumn reserve): out of depot liquidity, shown as a labelled overlay.
- One-line rule: an account is credit/non-free if EITHER balance < 0 OR type is `credit_line` — type wins.

### CM3 — Concentration thresholds (Jordan, 12:58)

- Default + per-call override, **instrument-type-aware** (flat threshold rejected — both blind spots and false alarms).
- Defaults on the STEERABLE basis (excl. crypto/Family/Guest/Zock):
  - Single stock: WARN > 7%, HARD > 10%.
  - Broad/diversified ETF (e.g. VWCE): exempt from single-name rule or own high threshold (~25%+) — a World ETF at 20% is target, not risk.
  - Category/theme (strategy leaf): flag when over-target > 3pp OR > 150% of target weight.
  - Asset-class cap: configurable.
- All thresholds shipped as defaults, overridable per call.
- **PM scope flags (to confirm):** (1) category/theme-leaf drift overlaps allocation/drift (FR-C territory) — proposed to DEFER out of FR-D Slice A. (2) "broad/diversified ETF" detection needs a data signal (asset class? per-security flag?) — open dependency.

### Scope resolution (Andi, 13:28)

- **[OPEN-B] RESOLVED:** ETF is already a distinct **asset class** in Andi's data → FR10 reuses it to exempt ETFs from the single-name rule. No new field. Cheapest path.
- **[OPEN-A] RESOLVED:** theme/category drift deferred out of FR-D Slice A → FR-C. MVP risk endpoint = single-name + HHI + asset-class caps.
- **New ideas parked to `addendum.md`** (future, NOT this PRD): (1) auto region/sector classification incl. ETF percentage look-through (candidate free source justETF.com); (2) region = market-exposure not HQ (revenue-based, e.g. Apple ≠ pure US); (3) risk-overlap/correlation matrix.
- **Strategic note:** FR-A, FR-C, and the overlap matrix all share ONE blocker — a free external data source (no paid providers). Recommended a single data-source feasibility spike to gate all three; current MVP needs none.

### Finalize (Andi chose "A", 13:40)

- **Reviewer gate:** rubric walker run → verdict **Good**, 0 critical / 2 high / 4 medium / 4 low. Full review in `review-rubric.md`.
- **High findings fixed:** FR8 now fixes Top-N default (N=10, overridable) and HHI scale (0–10000) + bands (low <1500 / moderate 1500–2500 / concentrated >2500) with output contract.
- **Medium findings fixed:** FR6 gives the explicit deployable-cash computation rule (Σ free_cash ≥ 0, EUR hub); FR9 specifies the cap-violation payload (class, current %, cap %, overage; caps opt-in, no defaults); added a **Glossary** canonicalising the deployable-cash term family + liquidity_role + steerable basis + HHI + settlement FX.
- **Low findings fixed:** FR1 names the three persisted fields; FR10 pins the ETF default (WARN > 25%, no HARD).
- **PM-chosen defaults (overridable, flagged to Andi):** Top-N = 10; HHI bands 1500/2500 (standard DOJ-style thresholds); ETF WARN > 25%. Andi to veto if unwanted.
- **Input reconciliation:** all inputs were conversational (Jordan's discovery answers) — captured in this log + addendum; no external source documents to reconcile.
- **Status:** `prd.md` set to `final`. Suggested next: `bmad-create-epics-and-stories`.
