# Decision log — PRD Data Import & Sync

2026-06-21
- Intent: CREATE, Fast path, short PRD (owner: Andi).
- Captured: PP is a migration bridge, long-term goal is to replace it. → PRD §2.
- Captured: comdirect/German-bank CSV = IST snapshot only; transaction-grade data
  is PDF-locked. → PRD §2, Feature B, OD-2.
- DECISION (owner): reconsider AGENTS "no PDF intake" policy; in-app PDF parsing
  is a candidate (Feature D), gated on OD-1. → PRD Feature D, §6.
- DECISION (owner): cover intake via generic mappable CSV (structured brokers),
  snapshot import, API/MCP write-parity (LLM push), own export/backup; keep
  market-data sync as-is. → PRD Features A/B/C/E/G.
- Constraints unchanged: no broker/bank live sync, no external LLM calls from the
  app, Decimal for money. → PRD §4, NFR-equivalent notes.
- Document language: English; conversation: German.
- Open: OD-2 (verify comdirect export), OD-3 (saved mappings MVP?).

2026-06-21 (later)
- DECISION (owner, overriding PM recommendation): OD-1 resolved as **Option A** —
  in-app broker-PDF intake, sandboxed/text-only/per-broker. PM had recommended
  Option B (out-of-app push); owner accepted the security/maintenance trade-offs.
  Captured in ADR-0021 (Accepted); AGENTS.md updated with the scoped exception;
  PRD Feature D updated from candidate to decided.

## 2026-07-25 — Validate run + UAT fix round

- **Reviewer gate (validate intent):** adversarial-general only; the rubric
  walker was deselected by the owner, so no dimension verdicts. Grade from
  severity counts: Poor (6 critical, 8 high, 5 medium, 1 low). Artifacts:
  `review-adversarial-general.md`, `validation-report.{html,md}`.
- **DECISION (owner, privacy override):** naming the operator (Andi) and
  naming comdirect in connection with him is **acceptable**. Out permanently:
  concrete financial values, and anything about family or household. The
  finding is therefore narrowed, not dismissed — what was kept is OD-2's
  missing handling rule, which is repo hygiene: the verification file is
  inspected locally and never committed, and only a synthetic fixture derived
  from its structure enters the repo.
- **DECISION (agent, flagged for review): FR ids prefixed `FR-DI-n`** instead
  of renumbering into the global sequence. The draft's FR-1..FR-17 collided
  with the authoritative sequence across at least eight identifiers, and ADRs
  cite global ids in prose (ADR-0029 uses FR-7 repeatedly in its main-PRD
  meaning). Prefixing keeps these requirements reorderable without consuming
  global ids.
- **DECISION (agent, flagged for review): Feature B reframed from "snapshot
  import" to "external position list for reconciliation".** All three storage
  representations are closed (ADR-0004 forbids stored holdings; synthesizing
  deliveries fabricates unconfirmed history; a new ledger kind is an ADR-grade
  change to a closed kind set). ADR-0029 §6 already built the invariant-safe
  option as a never-persisted reconcile endpoint, so Feature B is now a UI and
  CSV intake path over it. Snapshot *seeding* survives only as an explicitly
  confirmed conversion that states the cost-basis consequence.
- **Premise corrected:** §2's "hard truth about source data" is now stated as
  a **working hypothesis, unverified (OD-2)**, with the note that ADR-0021 was
  accepted and AGENTS.md amended before OD-2 was closed. A re-open trigger was
  added. OD-2 moved from step 2 to step 1 of the phasing.
- **Gap closed:** the official broker REST API — already committed in the
  founding PRD's FR-17 — was absent from both this PRD and ADR-0021's options
  analysis. Added as a "considered and not chosen" subsection with the three
  arguments that would still justify PDF intake (history depth, unattended
  operation, gate timing), all marked as arguments rather than evidence.
- **Applied, from findings:** FR-DI-19 added as a cross-cutting ADR-0029
  conformance requirement (identity ladder, preview→apply revalidation,
  config-at-risk acknowledgment, inverse check, fail-closed); FR-DI-2 value
  mapping and FR-DI-3 source format profile split out of the old FR-1, with
  sign normalization made explicit; FR-DI-6 replaced the one-line idempotency
  claim with the two-layer mechanism and four named re-import cases; Feature C
  given journaling, authorization (OD-4), identical-validation, batch
  all-or-nothing and idempotency-key precedence; Feature D given numeric
  sandbox limits, the real PDF threat classes, and a dependency PR scheduled
  before any story; FR-DI-18 rewritten to the rescoped `pg_dump` scope with
  the cancelled PP export removed; FR-DI-20 partial-apply semantics stated;
  the old FR-17 deleted into a follow-up note; scope restated positively so
  binary `.portfolio` and PP XML are named out rather than admitted by
  complement.
- **Acceptance criteria added to every requirement** — the document previously
  had none, so it could not pass its own ADR-0026 decision gate.
- **`addendum.md` written** — the header had pointed at a non-existent file
  since 2026-06-21.
- **Staleness fixed:** `updated` bumped to 2026-07-25; ADR-0027..0031 added to
  the reference set; the kind set corrected from 13 to 15; the FR-14
  double-definition and the #482 double-reference resolved; §8 turned into an
  issue→requirement table pointing at `epics.md` for the global mapping.
- **New open decision:** OD-4 (write authorization model for Feature C).
- Status stays `draft`. With acceptance criteria now present it is closer to
  decision-gate ready, but **OD-2 must be answered before Feature D**, and
  OD-3 before Feature A.

## 2026-07-25 (later) — OD-2 closed, OD-3 parked, PP XML deprioritized

- **OD-2 — RESOLVED (owner observation).** The bank's structured CSV export
  contains **holdings only, no trades**. This *confirms* the premise §2 had
  been demoted to an unverified hypothesis earlier today, so the re-open
  trigger does not fire and ADR-0021's Option A stands justified. §2 rewritten
  from hypothesis to confirmed finding; the assumption register updated.
  Two follow-on effects recorded: Feature D is now the primary route to
  transaction-grade German-bank data, and Feature B has a confirmed real input
  (the holdings CSV is exactly what the reconcile path consumes).
- **Split out of OD-2, still open:** whether the official broker REST API
  carries deep transaction history and can run unattended (founding PRD FR-17,
  OQ-6). Not urgent — it sits behind the Phase 3 gate — but if it later proves
  rich and unattended, Feature D's scope is worth revisiting before more
  per-broker parsers are written.
- **Process note kept in the PRD, deliberately:** ADR-0021 was accepted and
  AGENTS.md amended before this verification ran. The answer came back
  favourable; the sequencing was still backwards. Recorded so the next
  decision of this shape is not taken the same way — not as a criticism of the
  outcome.
- **OD-3 — open, no owner preference.** The owner has no current view on
  whether saved mappings (FR-DI-4) are MVP. Nothing is blocked: Feature A sits
  late in the phasing. The PRD now records the two arguments that will decide
  it (import friction vs. a smaller first-version idempotency surface) and
  defers the decision to Feature A kickoff.
- **DECISION (owner): PP XML (FR-DI-21) parked, no priority** — JSON v1 is the
  live data base. Scope gate unchanged if it is ever revived. Removed from the
  phasing sketch.
- **Phasing updated:** OD-2's verification step is done and removed; the
  remaining eight steps renumbered.
