# generated: 2026-07-25
# last_updated: 2026-09-15 (Sprint 11 and Sprint 12 both closed out, 0.11.0 and 0.12.0 prepared for the owner; E11's second alignment pass shipped, its tracker #356 stays open on the Sprint 13 remainder)
# project: portfolixir
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog -> in-progress: Automatically when first story is created (via create-story)
#   - in-progress -> done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
#
# PROJECT NOTE (2026-07-25):
# Epics 1-5 and 7-16 carry no story-level breakdown in epics.md -- their work
# unit is the GitHub issue (see the FR Coverage Map). Only E6, E17, E18 and E19
# have '##### Story' entries, so only those produce story rows here. Statuses
# were verified against `main` (ff4d809), the open-issue list AND open pull
# requests, not assumed -- an issue's state lags the PR that fixes it.

generated: 2026-07-25
last_updated: 2026-09-15
project: portfolixir
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

# ---------------------------------------------------------------------------
# CLOSE-OUT / RECONCILIATION LOG -- newest first
# ---------------------------------------------------------------------------
#
# CLOSED OUT (2026-09-15, Sprint 12) -- PR #813 rebase-merged 19:46 UTC,
# `main` at 5ea57ea3:
# Verification basis: the merge commits on `main` (d6466f7d..5ea57ea3, 28
# commits linear, no merge commits), the Actions runs on the merge push (CI
# 1525 and Commit authorship 539, both verified green before this entry was
# written), the post-merge issue list (30 open; each of the batch's
# twenty-two verified closed/completed), and the diff of the merged tree
# against the pre-merge head ceef8665 (empty) -- not this file's prior
# claims. The rebase-merge replayed the commits, so main's SHAs differ from
# the branch's; tree equality is the check that holds.
#
# WHAT CLOSED: twenty-two issues by the merge's keywords -- #784-#805, the
# whole contiguous run, across Lanes F (defect sweep), N (data notes and
# freshness), S (Tax, Snapshots, Views, sunburst, income chart), K (Wealth
# KPI band, Overview strip), P (phone rows, filter sheet), T (booking
# drawer) and D (detail pane, tree rows, custom range). Closed by hand:
# none. E11's tracker #356 stays OPEN deliberately -- the 2026-09-12
# review's remainder (#806-#809) is declared Sprint 13 work by D-3 and the
# closing act's three findings (#815 #816 #817) attach to the same tracker,
# so the epic is not finished and closing it would lose the thread.
#
# TWO-WAY COVERAGE -- the rule fired for the first time, on this batch's own
# removal. #803 removed the Transactions "Current holdings" panel as a
# duplicate of Wealth -> Holdings; that panel carried the column picker #732
# added as the human half of the holdings API's `fields=` sparse fieldset
# (verified in the diff: @holdings_column_defaults, @holdings_column_keys and
# the #holdings-panel section with its #732 comment all left with it). The
# projection's valuation fields are now API/MCP-only. Allowed for the batch
# that creates the gap provided the PR says why -- #813's briefing does --
# with the human view due in the same or the NEXT epic batch. This batch is
# the "same"; SPRINT 13 IS THE "NEXT", and its close-out must find #814
# shipped or record a finding. #814 was filed the day the gap was created
# (15:48 UTC), under the scope-lock rule. Nothing else is agent-only:
# #798's newest_quote_date shipped with its human view in the same commit.
#
# SURFACE CHECK: no new read-ergonomics parameter landed this batch, so the
# parameter-family sentence has nothing to enumerate. The one payload field
# it added passes the check's spirit on both scopes rather than one:
# newest_quote_date is on the portfolio scope AND the view scope, in the API
# (json.ex:772 and json.ex:821) and in both MCP tools
# (portfolixir.portfolios.valuation and the bucket-view valuation) -- the
# same family FR-37 shipped half of, on the portfolio scope only, which
# became #740. Not repeated here. The surface gap the batch did leave is a
# human one and is filed: the phone filter sheet landed on the securities
# list (#800) and not on the transaction history (#816).
#
# CLOSING ACT: three adversarial roles (correctness hunter, edge-case
# hunter, design critic) plus the UAT persona walkthrough under D-4's
# conditions -- DE, 1440 px and 390 px, light and dark, on the committed
# seed script, a booking walked end to end through the drawer at 390 px.
# Thirty-nine route renders, no horizontal overflow, no console or page
# errors. Twenty-nine confirmed findings fixed on the branch in one commit
# (9a3abc20); three recorded as issues rather than widened (#815 #816 #817).
# The findings that mattered: the Overview strip's stale count disagreed
# with the list it links to; a retired security's old close was still
# becoming a day change on three surfaces; the tree printed 0.00 while
# holdings loaded and summed an unpriceable position as zero; the note
# editor survived a change of security; seven date renders printed ISO in
# German; the not-computable dash rendered at value weight.
#
# PROCESS FINDINGS (detail in sprint-12-retro-2026-09-15.md): the
# patch-coverage listing earned its place as a review input the first batch
# it was one -- it found a missing meta-test (714d2126) and a defaulted
# `today` whose Date.utc_today() fallback would have answered a
# local-calendar question in UTC (5ea57ea3, #609), the second a wrong path
# rather than an untested one. Both landed AFTER promotion, as Sprint 11's
# did: the listing arrives with the CI run on the promotion commit, so the
# fix is ordering (read the last pre-promotion run while the closing act is
# open), not more discipline. The rebase over the sibling close-out worked
# as the plan predicted; writing a log entry's closing clause in the future
# tense is what made an edit necessary at resolution time.
#
# LANE M: nothing applied -- every Hex and npm dependency at its latest
# release, both audits clean, and the three Dependabot PRs the plan named
# (#781, #782, #775) already resolved by Sprint 11's lane the same morning.
# Six rows deliberately not updated with reasons (Elixir/OTP #727, the Node
# runtime image #782, the @types/node major, the Dockerfile HEX_VERSION, the
# PostgreSQL image, the BMAD module SHAs) in
# version-report-2026-09-15-sprint12.md.
#
# TAG: 0.12.0 is an OWNER ACTION (D-5) -- the agent's credential cannot push
# tags. The annotated tag command on the merged head 5ea57ea3 is in the
# retrospective's close-out ledger.
#
# NEXT: Sprint 13 is unplanned. Its gate has four declared candidates
# (#806-#809), one coverage debt with a deadline (#814, due this batch), the
# Sprint 11 surface-check outlier (#811) and this batch's three findings
# (#815 #816 #817).
#
# BATCH OPENED (2026-09-15, Sprint 12) -- branch agent/claude/ux-alignment-2
# from main at c2164f54 (the commit that merged Sprint 11's batch, PR #810 --
# D-1 satisfied), the draft PR opened with this commit (ADR-0026 step 2; the
# plan's Lane Z: registry rows when the branch opens). Rows touched here: the
# FR Coverage Map's UX-DR row names #784-#809, UX-DR27 and UX-DR2 as amended
# 2026-09-14, and the Tracker Index's E11 line names the 2026-09-12 review as
# its second reference; the close-out reconciles the rest against the merge,
# not against this note. Lane order per the plan: F, N, S, K, P, T, D, with
# M at lane time and the shrink order cutting from D upward. The Sprint 11
# close-out (PR #812) carries its own entry below, merged 2026-09-15 19:38
# UTC; this branch was rebased over it then, which is why its rows sit above.
#
# CLOSED OUT (2026-09-15, Sprint 11) -- PR #810 rebase-merged 12:16 UTC,
# `main` at c2164f54:
# Verification basis: the merge commits on `main` (cc70b9f..c2164f54, 23
# commits linear per the ADR-0026 rebase-merge amendment), the Actions runs
# on the merge push (CI 1506, Commit authorship 520, verified green before
# this entry was written), and the post-merge open-issue list (48 open) --
# not this file's prior claims. Branch opened, built, reviewed, promoted and
# merged inside six hours; the plan had been adopted eight days earlier.
#
# WHAT CLOSED: six issues by the merge's keywords (#772 Bandit, #382 CSP and
# the HTTPS posture, #776 the limit surface, #779 and #610 performance on
# retired securities, #572 the benchmark comparison per ADR-0046) --
# verified against the open-issue list, none remained open. Closed by hand:
# none -- the sprint had no tracker of its own (Lane Z: no new epic), and
# E21's tracker #757 was closed in Sprint 10. E21's remainder is shipped:
# nothing in the family stays open. E10 moves to in-progress on its first
# shipped item. Stays open with a reason: #727 (both toolchain halves still
# blocked upstream; triggers re-checked 2026-09-15 at lane time, none fired,
# #775 closed with the reason); #811 (the journal read's own limit parser
# clamps where ListLimit refuses -- filed by the surface check, a Sprint 13
# candidate); #314 (the ratchet rides every batch: patch coverage 96.7 % on
# the merged head, project 91.64 %).
#
# TWO-WAY COVERAGE CHECK: Lane B shipped both halves in one batch (the two
# API reads and their MCP tools, then the Wealth page's picker, overlays and
# comparison block, DE and EN); Lane X's price_date and stale_priced_count
# are on the valuation read and the Wealth finding names the holdings on the
# page; Lane W's bounds are a machine-read parameter with no human view by
# nature, as #771 was treated in Sprint 10; Lanes C and D add no capability
# surface. The ledger stays empty, and no earlier batch's capability is
# overdue a human view.
#
# SURFACE CHECK (AGENTS.md step 5 clause, third use): `limit` is carried by
# GET /transactions, /securities, /exchange_rates and
# /securities/:id/quotes (Sprint 10) and, since this batch, by
# /securities/:security_id/notes, /notes/unreviewed, /notes/uncorroborated,
# /notes/expiring, /snapshots, /realized_gains, /external_flows and /costs,
# each echoing the limit it applied, their MCP twins the same;
# /securities/:security_id/trades is bounded by from/to and says so in its
# basis; /journal parses its own limit and clamps instead of refusing --
# the one outlier, filed as #811, not changed in the batch. Checked against
# the route table, not the briefing.
#
# AFTER PROMOTION: one tests-only commit rode the PR (c01aa83, rebased as
# c2164f54) after the Codecov report on the promoted head listed 45 changed
# lines without a test -- above the 90 % patch target, so no gate was red,
# but half of the lines were documented behaviour paths, several in
# risk-tier code (the engine's GBX pricing, its unpriced days before the
# first stored FX rate), that four review roles had not asked for. Pinned
# with exact figures; the retrospective carries the process finding.
#
# TAG: 0.11.0 is an OWNER ACTION (D-4, ADR-0026 step 5 as amended on PR
# #780): the annotated tag command on the merged head c2164f54 is in
# sprint-11-retro-2026-09-15.md under "Close-out ledger". Not attempted from
# the session.
#
# RETROSPECTIVE: sprint-11-retro-2026-09-15.md. Process findings: the
# closing act's inputs did not include the patch-coverage listing, and the
# untested risk-tier paths it named after promotion are the cost (standing
# rule from here: the listing is an input to the hunters, the briefing names
# what stays uncovered); the check-in was re-armed on every firing and its
# prompt updated after promotion, so the watch never lapsed (Sprint 10's
# finding, acted on); the history cleanup for the rebase-merge folded three
# fix-ups into their commits with a byte-identical tree; the screenshots
# went into a PR comment from the start (Sprint 10's finding, acted on);
# nothing died on a session limit; Codecov's check runs appeared on the
# second head only, so heads are verified from the check runs, never from
# the comment.
#
# LANE M: version-report-2026-09-15.md, written at lane time. Applied, each
# its own commit: the Bandit swap's dependency moves (cowboy, cowlib and
# plug_cowboy removed), mint 1.10.0 (two advisories the unpinned Hex
# surfaced), phoenix 1.8.14, dialyxir 1.4.8 (supersedes #781), the MCP
# lockfile (hono 4.13.8 for three moderate advisories). Deliberately left,
# with reasons: Elixir/OTP (#727; #775 closed), the Node 26 image (#782
# closed -- Current, not LTS, until October), the @types/node major, the
# Dockerfile Hex pin, the PostgreSQL image, the BMAD modules. Audit state at
# lane time: hex.audit, deps.audit, npm audit, sobelow (no ignore) and
# dialyzer clean.
#
# BATCH OPENED (2026-09-15, Sprint 11) -- branch
# agent/claude/perimeter-remainder-benchmark from main at cc70b9f, the draft
# PR opened with this commit (ADR-0026 step 2; the plan's Lane Z: registry
# rows when the branch opens). Sprint 12's D-1 holds: its branch
# agent/claude/ux-alignment-2 opens on this batch's merge. Rows touched here:
# the FR Coverage Map's FR-9 row now names #572 and ADR-0046, and the Tracker
# Index's E10 line names the benchmark comparison as its first shipped item;
# the close-out reconciles the rest against the merge, not against this note.
#
# PLANNED (2026-09-12, Sprint 12) -- sprint-plan-2026-09-12-sprint12.md,
# adopted by the merge of PR #783 (the planning PR that also carries the
# whole-surface UX review ux-review-2026-09-12.md, the 2026-09-12 spine
# amendment with UX-DR27, and seventeen mockups). E11's second alignment
# pass under tracker #356, twenty-five sub-issues #784-#808, #809 added 2026-09-14. Lanes: F
# (defect sweep #784 #785 #786 #788 #790 #791), N (data notes + freshness
# #792 #789 #787), S (Tax #795, Snapshots #796, Views #802, sunburst #793,
# income chart #794), K (#797 #798, picks), P (#799 #800, picks), T (#803,
# pick), D (#804 #805 #801, picks), M (#781 apply, #782 close with the LTS
# reason), Z (registry rows when the branch opens). D-1: Sprint 11's batch
# runs first, Sprint 12's branch opens on its merge (Lane F may go ahead as
# a small PR by owner comment). D-2: silence = the recommended variant;
# picks made 2026-09-14: K A/B, P A/B, T C, D A/A/A (#798-B rides Lane K,
# UX-DR2 amended on PR #783); #809 filed the same day (Sprint 13 candidate).
# D-3: #806 #807 #808 and #809 are Sprint 13 candidates. D-4: the closing
# act runs under the #706 conditions with the review's seed script, which
# moves into priv/demo/ in the batch. D-5: 0.12.0 command in the close-out.
#
# PLANNED (2026-09-07, Sprint 11) -- sprint-plan-2026-09-07-sprint11.md,
# adopted by the merge of PR #780 (the merge is the signature -- ADR-0026
# amended on the same PR; the sprint tag became an owner action). Lanes: D (#772 Bandit),
# C (#382 CSP + HTTPS posture, D-2), W (#776 limit surface), X (#779 + #610,
# performance on retired securities, D-3, risk-tier), B (#572 benchmark per
# ADR-0046, D-1, risk-tier), M (#727 triggers; Dependabot #774/#775), Z (no
# new epic -- E21 remainder, E1/E5 correctness, E10's first item). D-4 made
# the sprint tag an owner action in ADR-0026 step 5. Filed by the plan: #779.
#
# TAG 0.10.0 RESOLVED (2026-09-07): annotated tag on 8d178e35 (one commit past
# the close-out's b701613a -- #777, the session-lifetime amendment to
# ADR-0045 -- the F4 pattern: it marks the right software), release published
# 2026-09-07 17:24 UTC through the Release workflow. Verified against the
# remote tag object and the release list. #777 is not in the Sprint 10 retro's
# closed-issue ledger because it merged after the retro was written; it is
# recorded here instead of by editing the retro.
#
# CLOSED OUT (2026-09-06, Sprint 10) -- PR #773 rebase-merged, `main` at
# b701613a:
# Verification basis: the merge commits on `main` (22913af..b701613a, 25
# commits linear per the ADR-0026 rebase-merge amendment), the Actions runs
# on the merge push (CI 1483, Commit authorship 497, verified green before
# this entry was pushed), and the post-merge open-issue list -- not this
# file's prior claims. Adopted, planned, built, reviewed and merged inside
# 14 hours of the triage's adoption.
#
# WHAT CLOSED: fourteen issues by the merge's keywords (#758 #759 #760 #761
# #762 #763 #764 #765 #766 #767 #768 #769 #770 #771 -- verified against the
# open-issue list, none remained open). Closed by hand: #757, the E21
# tracker. E21 is DONE: every lane of the triage's Part 3 that the batch
# claimed shipped -- perimeter, egress bounds, provenance, import
# robustness, web/API hygiene, the optional UI login of ADR-0045. Stays
# open with a reason: #382 (CSP) and #772 (Bandit, which removes cowlib and
# its three advisories) are the next batch by the triage's own ruling;
# #727 (both toolchain halves blocked upstream, triggers re-checked at lane
# time, none fired).
#
# TWO-WAY COVERAGE CHECK: the batch's agent-visible capabilities (the list
# limits, the contract manifest v3 entry, the provenance refusals, the token
# policy) shipped on API and MCP together; the UI login, the confirmations
# and the imports skip report are human-only by nature and say so in the
# briefing. No capability shipped agent-only. The ledger stays empty, and
# no earlier batch's capability is overdue a human view.
#
# SURFACE CHECK (AGENTS.md step 5 clause, second use -- and the first time
# it caught something): `limit` is carried by GET /transactions,
# /securities, /exchange_rates and /securities/:id/quotes plus their four
# MCP tools; /journal carries its own older bound. It is NOT carried by
# /securities/:id/notes, /notes/expiring, /notes/uncorroborated,
# /notes/unreviewed, /securities/:id/trades, /realized_gains, /snapshots,
# /external_flows or /costs, whose collections grow with the ledger. The PR
# briefing claimed the family was complete; checked against `mix phx.routes`
# at close-out it was not. Filed as #776. Second read-ergonomics family to
# ship half-done after FR-37 (#740).
#
# TAG: 0.10.0 is an OPEN OWNER ITEM, created locally as an ANNOTATED tag on
# the merged head b701613a and refused by the session's git proxy with HTTP
# 403 on the push -- the fourth sprint in a row, and the proxy's own status
# shows no relay failure, so the refusal is the session credential's branch
# scope rather than an egress block. No create-tag or create-release call
# exists on the session's GitHub API surface either. The prepared command
# is in sprint-10-retro-2026-09-06.md, with the standing question of
# whether ADR-0026 step 5 should name the tag an owner action.
#
# RETROSPECTIVE: sprint-10-retro-2026-09-06.md. Process findings: an
# invariant test that pinned an attribute rather than the behaviour left
# every destructive confirmation inert for the life of the feature, found
# only by a browser probe; the new Host guard refused our own MCP companion
# inside Compose; the review agents died on a session limit again (Sprint
# 9's recorded lesson, unacted); the codecov patch check was the only red
# check and was right twice; a URL written into a PR body through the API
# comes back escaped, so closing-act screenshots belong in a comment; the
# demo strategies seed still assumes a portfolio name; the session's own
# check-in disabled itself after firing and the PR watch lapsed silently.
#
# LANE M: version-report-2026-09-05.md, written at lane time. Nothing
# applied -- every Hex row was current two days after the Sprint 9 lane and
# no Dependabot PR was open. Deliberately not updated, with reasons: both
# toolchain halves (#727), cowlib (no fixed release; the Bandit swap is
# #772), the PostgreSQL image, @types/node (major-wide ignore), the BMAD
# externals. Not a dependency but owned by the lane: the MCP image moved
# from the floating node:24-alpine to an exact tag, the new production image
# pins its bases, and Dependabot's docker ecosystem now covers both.
#
# TAG 0.8.0 RESOLVED (2026-09-03): the owner ran the prepared command -- the
# tag is an ANNOTATED tag object on ea6617a (the ADR-0044 draft commit, a
# docs-only delta over the batch head 125d656: the F4 pattern, it marks the
# right software), and the tag push created release 0.8.0 through the Release
# workflow (published 2026-09-03 12:24 UTC). First annotated sprint tag under
# ADR-0026 step 5; the Lane Z guard shipped in Sprint 8 had nothing to say,
# which is the point. Verified against the remote tag object type and the
# release list, not against this file's prior claim.
#
# CLOSED OUT (2026-09-03, Sprint 9) -- PR #754 rebase-merged, `main` at 6de32ff:
# Verification basis: the merge commits on `main` (8771702..6de32ff, 17
# commits linear per the ADR-0026 rebase-merge amendment), the Actions runs
# on the merge push (CI 1467, Commit authorship 481, verified green before
# this entry was pushed), and the post-merge open-issue list -- not this
# file's prior claims. Planned, built, reviewed and merged on the day
# ADR-0044 was signed.
#
# WHAT CLOSED: ten issues by the merge's keywords (#747 #748 #749 #750 #751
# #752 #740 #741 #738 #737 -- verified against the open-issue list, none
# remained open). Nothing needed closing by hand. E20 is DONE: every
# ADR-0044 scope item shipped (entry, derived state, four reads, timeline,
# journal arming, contract read), nothing in the shrink order was cut. Stays
# open with a reason: #727 (both toolchain halves blocked upstream; the
# triggers re-checked against excoveralls 0.18.5, Elixir 1.20.4 and ecto
# 3.14.2 -- none fired).
#
# TWO-WAY COVERAGE CHECK: every agent-visible capability of this batch (the
# research log and its four reads, the contract read, the backfill scope,
# include_positions on the view valuation, min_drift on position targets)
# shipped WITH its human view in the same batch. The ledger stays empty.
#
# SURFACE CHECK (AGENTS.md step 5 clause, first use): include_positions is
# carried by GET /portfolios/:id/valuation, /allocation and /views/:id/
# valuation; min_drift by /allocation and /position_targets; fields= by
# transactions, holdings, securities; since= by transactions, securities and
# the contract read. Every endpoint of these families carries its parameter.
#
# TAG: 0.9.0 is an OPEN OWNER ITEM, created locally as an ANNOTATED tag on
# 6de32ff and refused by the session's git proxy (third sprint in a row --
# recorded in the retro as a pattern, with two follow-up options). The
# prepared command is in sprint-9-retro-2026-09-03.md.
#
# RETROSPECTIVE: sprint-9-retro-2026-09-03.md. Process findings: the three
# parallel review agents died on a session limit before reporting and the
# closing act ran as a stated self-review -- check the budget before fanning
# out; the codecov patch check was the only red check and pinned edge cases
# worth having; the demo strategies seed assumes a portfolio name.
#
# LANE M: version-report-2026-09-03.md, written at lane time. Applied rows in
# their own commits (phoenix 1.8.13, telemetry_metrics 1.2.0, setup-node v7,
# phoenix_live_view/req/ecto patches, tsx/zod in range); deliberately not
# updated, with reasons: both toolchain halves (#727), @types/node 24->26
# (declined major-wide in dependabot.yml), cowlib, the BMAD externals.
#
# SPRINT 9 BATCH OPENED (2026-09-03): the Lane Z registry row -- epic-20 above,
# the E20 Tracker Index line and the FR-45 issue column in epics.md -- lands
# with the batch branch, as the plan prescribes. The close-out entry follows
# the merge; nothing here claims the batch is done.
#
# SPRINT 9 ADOPTED (2026-09-03): sprint-plan-2026-09-03-sprint9.md, drafted
# the day ADR-0044 was signed (4c4cc9e) and adopted by the owner on PR #753
# the same day, D-1 (#737 fix shape) signed with it. Lanes: the knowledge batch (#747
# tracker, #748-#752), the agent-round debt (#740, #741, #738), the historical
# FX gap (#737, D-1 on the plan), maintenance (the npm audit finding that
# turned `main` red on run 1460, fixed in the plan PR's first commit; the
# Dependabot rows; #727 triggers), and the E20 registry row, which lands when
# the batch branch opens -- no epic-20 key yet, so this file does not claim a
# batch that has not started.
#
# CLOSED OUT (2026-08-22, Sprint 8) -- PR #735 rebase-merged, `main` at 125d656:
# Verification basis: the merge commits on `main` (5fe43f6..125d656, 24
# commits linear per the ADR-0026 rebase-merge amendment), the Actions run on
# the merge push (1445, verified green before this entry was pushed), and the
# post-merge open-issue list -- not this file's prior claims.
#
# WHAT CLOSED: fourteen issues by the merge's keywords (#731 #732 #717 #718
# #719 #720 #721 #723 #729 #730 #724 #725 #726 #728 -- verified against the
# open-issue list, none remained open). Nothing needed closing by hand.
# Sprint 8 had no dedicated epic tracker; the standing trackers (#356, #417,
# #418, #419, #420, #470) stay open by design. Stays open with a reason:
# #727 (BOTH toolchain halves blocked upstream -- the OTP move on dialyzer
# opaqueness, the Elixir move on :cover being unable to instrument 1.20.3
# BEAMs so the coverage gate cannot run; evidence and re-check triggers on
# the issue), #737 (no path to store a historical FX rate; filed from Lane
# C), #738 (README predates Snapshots/Tax/performance; filed at close-out).
#
# TWO-WAY COVERAGE CHECK: the Sprint-6 debt (#731 ?since= human view, #732
# fields= column picker) is DISCHARGED inside its deadline. The batch's new
# agent-visible capabilities (three Cash-flow facet endpoints + MCP tools,
# fields= on securities) shipped WITH their human views in the same batch --
# no new debt enters this ledger.
#
# TAG: 0.8.0 is an OPEN OWNER ITEM, prepared but not pushed (the session's
# git proxy allows pushes to the designated branch only). The prepared
# command (in the retro) creates it ANNOTATED on 125d656. First sprint under
# the Lane Z guard: a lightweight tag now announces itself in the release
# body instead of being discovered at the next close-out.
#
# RETROSPECTIVE: sprint-8-retro-2026-08-22.md. The process finding worth
# repeating here: "green" means CI on the branch, never a local run -- the
# Elixir 1.20.3 bump broke the coverage gate and stayed invisible for six
# and a half hours because the local toolchain (1.18.3) kept passing while
# every CI run failed. A session joining a branch checks that branch's CI
# state before building on it. Second finding: two sessions built the same
# sprint in parallel (#735/#736); an open PR claiming the same closing
# keywords is a stop sign at session start.
#
# LANE M: version-report-2026-08-22.md, written AT LANE TIME (Sprint 7's
# miss, not repeated). Applied rows in their own commits (phoenix,
# phoenix_live_view, req; the Node 24 pin); deliberately not updated, with
# reasons: both toolchain halves (#727), @types/node 24->26 (declined --
# types follow the pinned runtime; 26 is not LTS until October), tea/cis/bmb
# and cowlib per the report.
#
# CLOSED OUT (2026-08-19, Sprint 7) -- PR #716 rebase-merged, `main` at 80d3e7e:
# Verification basis: the merge commits on `main` (bb3d728..80d3e7e, 37 commits
# linear per the ADR-0026 rebase-merge amendment), the Actions runs on those
# commits, the open-issue list after the merge, and the remote tag list -- not
# this file's prior claims.
#
# WHAT CLOSED: sixteen issues by the merge's keywords (#706 #702 #700 #701
# #703 #704 #707 #414 #672 #705 #710 #711 #712 #709 #708 #722 -- verified
# against the open-issue list, none remained open), #471 by hand on
# 2026-08-18 (invalidated by ADR-0024, defect no longer exists), and #321 by
# hand at this close-out (superseded by ADR-0042; its working agreement was
# preserved into AGENTS.md by Lane Z). Sprint 7 had no dedicated epic
# tracker; the standing trackers (#356, #417, #419, #420) stay open by
# design.
#
# TAG: 0.7.0 RESOLVED 2026-08-20 -- the owner published the GitHub release
# (19:32 UTC, notes as prepared), which created the tag via the release UI:
# LIGHTWEIGHT, on d745712 (the #733 bookkeeping merge, a docs-only delta over
# the batch head 80d3e7e -- the F4 pattern: the tag marks the right software).
# That is the THIRD lightweight sprint tag against ADR-0026 step 5's
# "annotated", and the failure mode is now identified as structural rather
# than forgetfulness: the release UI's "create new tag" cannot produce an
# annotated tag, and the agent's session proxy cannot push tags at all, so
# the only path that satisfies the rule is the owner running the prepared
# command BEFORE creating the release. Carried into the Sprint 8 draft as a
# small Lane Z item (a release-workflow warning on lightweight tags) instead
# of re-prescribing the same command a fourth time.
#
# RETROSPECTIVE: sprint-7-retro-2026-08-19.md. The one process finding worth
# repeating here: the closing act ran TWICE, and the second pass found a
# reachable LiveView crash (recovery_label/1 without a :not_comparable
# clause) defended by a comment claiming unreachability -- verified against
# the render guard's inputs, not against the state's producer. The habit to
# keep: re-derive "unreachable" from the producer.
#
# DEBT WITH A DEADLINE (two-way coverage rule): #731 (?since= human view)
# and #732 (fields= column picker) are due by the end of the NEXT batch;
# their absence there is a close-out finding by the rule's own terms.
# Walkthrough findings outside the batch's scope: #729, #730.
#
# LANE M: applied rows in their own commits (five Actions majors, zod 4,
# TypeScript 7); the deliberately-not-updated list including the toolchain
# and BMAD rows is version-report-2026-08-19.md -- written at close-out,
# one step later than the lane intended (the done-list caught it, nothing
# else did).
#
# CLOSED OUT (2026-08-18, PR #714 planning layer; recovered 2026-08-20 from
# PR #715, which was closed unmerged -- it was based on the pre-batch `main`
# and edited the region this log rewrote twice since, so its CONTENT is
# preserved here and the PR itself is closed with a pointer):
#
# PR #714 was squash-merged as bb3d728 (single-concern planning PR; the
# rebase-merge rule of the ADR-0026 amendment applies to epic-batch PRs, not
# to this). What landed: the 2026-08-17 reconciliation (F4-F8), ADR-0042
# Accepted (one planning structure -- written once on the premise the tracker
# set was authoritative because the work sits there, then REWRITTEN after the
# owner disposed of that in one sentence: where data sits is not who reads
# it), ADR-0043 Accepted (a gate-closing ADR names its asks; closes F8),
# human-view-debt-2026-08-17.md CORRECTED (two unbuilt surfaces, not "mostly
# discharged"), and the adopted Sprint 7 plan.
#
# Its closing act ran four roles (evidence, consistency, scope/privacy,
# adversarial); thirteen confirmed findings, all fixed before promotion. It
# closed no issue by design, and carried NO TAG, deliberately: step 5's
# annotated X.Y.Z is a minor bump per sprint on the epic BATCH, and #714 is
# the planning layer that precedes it, exactly as #685 preceded #688.
#
# RETROSPECTIVE (from that round, worth keeping):
#   1. A document that asserts backlog state from another document is wrong
#      twice a day -- F5 and the #672 re-ask were the same defect in two
#      costumes, and the 2026-08-15 triage had already named the pattern.
#   2. A self-audit that shrinks the auditor's own work needs an adversarial
#      check before it is acted on -- the human-view analysis cut Sprint 7's
#      scope on an inverted reading, written by the agent that would
#      otherwise have had to schedule the work.
#   3. A reviewer's finding is evidence, not a verdict -- the #672 exclusion
#      quoted a section a later round of the same document had closed, and
#      the consistency reviewer said exactly that, and it was missed.
#
# MIGRATED (2026-08-18, Sprint 7 Lane Z) -- ADR-0042 executed:
# The story rows are gone from `development_status` -- the five schema-invalid
# `6-dx-*` / `6-c-1` keys plus the E17/E18/E19 story keys, 19 rows in total.
# `epic-N` and `epic-N-retrospective` are KEPT, per ADR-0042 section 4 as
# corrected by the 2026-08-17 agentic review: `development_status` is a required
# key, `cmd_status` fails on an empty one, and the retrospective-completion
# ledger has no other home -- Sprint 7's own close-out would have been the first
# casualty of deleting it.
#
# `sprint_plan.py validate` now returns valid: true, problems: []. It returned
# valid: false with those same five keys before this pass, which is the evidence
# ADR-0042 was argued on; the file had been invalid since the E6 batch and no
# gate caught it.
#
# `sprint_plan.py status` still works -- the capability ADR-0042 explicitly
# keeps. Its recommendation changed from `19-7-forward-projection-and-tax-bucket`
# (the one story deliberately deferred behind its own gate, which is the wrong
# answer ADR-0042 cites) to `bmad-retrospective`. Epic counts and the
# retrospective ledger read unchanged: 6 in-progress / 4 backlog / 9 done,
# 3 retrospectives done.
#
# EXPECTED, NOT A REGRESSION: `status` now reports six risks of the form
# "in-progress epic 'epic-N' has no stories". With no story rows in the file by
# design, every in-progress epic reports one. Recorded here so a later
# reconciliation does not read it as drift and re-add rows this decision removed.
#
# `epics.md` in the same pass: Epic List and Epic Detail replaced by a Tracker
# Index (one line per epic -- name, tracker where one exists, intent); all
# `##### Story` rows removed; Requirements Inventory, FR Coverage Map, scope
# ladder boundaries and the dated reconciliations kept. #321's working agreement
# preserved into AGENTS.md before the issue is closed by hand (invalidated, not
# implemented, so no closing keyword). ADR-0027/0028/0029/0031 and
# pr-review-checklist.md repointed off the deleted sections. ADR-0039 gained its
# gate-B3.2 ask list, the one retroactive application ADR-0043 allows.
#
# RECONCILED (2026-08-17, `main` at 1a88ad5) -- pre-Sprint-7 planning readiness:
# Verification basis: the commits on `main` (f0beb8c..1a88ad5), the remote tag
# list, the GitHub release list, the open-issue list (40 open), the open-PR list
# (8, all Dependabot) and the Actions runs on `main` -- not this file's own
# prior claims, two of which turned out to be wrong (F5, F6).
#
# WHAT RESOLVED since the Sprint 6 close-out:
#   - The two tags that close-out carried as OPEN OWNER ITEMS now exist on the
#     remote: `0.5.0` and `0.6.0`. `0.6.0` produced the GitHub release "Sprint 6:
#     agent surface, durable derived values, UI alignment" (published
#     2026-08-14T19:59Z) -- the first real exercise of the #659 release
#     automation. F1 of the 2026-08-12 pass (Sprint 5 had no tag at all) is
#     therefore CLEARED, and replaced by the narrower F4 below.
#   - c7877b0 -- owner feedback triage 2026-08-15 (ADR-0038). This is the
#     acceptance round for Sprint 6's design lane, not a fresh backlog: the owner
#     was looking at five surfaces that had shipped days earlier. It produced 13
#     issues (#700-#712) and 4 accepted decisions.
#   - 1a88ad5 -- ADR-0041 (category result) Accepted; slice one filed as #712.
#   - 93179b9 -- BMAD badge follow-up to #674.
#   - CI green on `main` through 1a88ad5 (CI + Commit authorship + pages).
#
# DECISION GATES now standing open for scheduling (ADR-0026 step 1 satisfied --
# all four signed off, so a Sprint 7 batch may draw on any of them):
#   - ADR-0027 amendment (transaction costs in the snapshot comparison) -> #708
#   - ADR-0039 amendment (refresh on the invalidating write, coalesced) -> #710,
#     plus #711 (measure and activate); #710 is risk-tier
#   - ADR-0040 (explicit unallocated remainder in target plans) -> #709
#   - ADR-0041 (category result, money-weighted roll-up) -> #712
#
# SPRINT 7 OBLIGATIONS ALREADY COMMITTED -- these are not open candidates:
#   - The two-way coverage rule: FR-37 and FR-38 shipped agent-only in Sprint 6.
#     Their HUMAN VIEW IS DUE IN THIS BATCH. Absence after Sprint 7 is a
#     close-out finding by the rule's own terms.
#     SCOPE CORRECTED 2026-08-17 by the agentic review: an earlier draft of
#     `human-view-debt-2026-08-17.md` called most of this discharged. It is not.
#     `fields=` is implemented on the TRANSACTIONS and HOLDINGS endpoints, and
#     neither list has a column picker; the securities picker predates FR-37 and
#     sits on the one list with no `fields=`. So the obligation is TWO unbuilt
#     surfaces (field selection, `?since=`) plus a partial threshold half -- it
#     is wider than the reconciliation first recorded, not narrower.
#   - Sprint 6 OQ-2: the UI was left ALIGNED BUT NOT FINISHED on the stated
#     understanding that Sprint 7 is the closer. See F5 for what that remainder
#     actually is.
#
# FIVE FINDINGS:
#
#   F4 -- TAG HYGIENE, two real deviations and one non-issue. ADR-0026 step 5
#   prescribes an ANNOTATED tag; both `0.5.0` and `0.6.0` are LIGHTWEIGHT
#   (`git cat-file -t` returns `commit` for both). And `0.5.0` has NO GitHub
#   release -- it was pushed before ab9851f widened the Release workflow trigger
#   to the bare-number scheme, so the Sprint 5 boundary has a rollback point but
#   no communicable changelog. NOT a finding, recorded so it is not re-raised:
#   each tag sits a little after the commit its close-out named -- `0.5.0` on
#   47dd988, TWO commits after 73affc5; `0.6.0` on aa4a38a, THREE commits after
#   f0beb8c (bfb9a9f, ab9851f, aa4a38a) -- but every commit in both deltas is a
#   planning/docs/meta-test commit with ZERO application-code change. The tags
#   mark the right software.
#
#   F5 -- #560 WAS ALREADY CLOSED when the Sprint 6 close-out and retro named it
#   as Sprint 7 work. It closed 2026-08-10 via PR #656 (income charts scroll
#   horizontally on narrow viewports) -- four days BEFORE the close-out was
#   written. The UX-DR remainder for Sprint 7 is therefore #414 and #672, plus
#   whatever #707 (the design engagement) specifies; it is NOT "#414 and #560".
#   Corrected in the retro with a dated note rather than silently rewritten.
#
#   F6 -- THIS FILE AND epics.md WERE STALE against 2026-08-15. Neither carried
#   any of #700-#712, and the FR Coverage Map carried neither ADR-0040 nor
#   ADR-0041. Planning off either document would have missed 13 of the
#   candidates. Fixed in this pass; epics.md gains a matching section.
#
#   F7 -- F2 (2026-08-12) IS UNCHANGED AND NOW WIDER. Section J (FR-37..FR-48)
#   still hangs off no epic row, the Epic List still ends at E19, and the 13 new
#   issues also have no epic home -- they hang off the GitHub trackers (#417,
#   #419, #420, #356) instead. Two parallel epic structures remain, and none of
#   the 2026-08-15 work resolves this.
#   **RESOLVED 2026-08-17 by ADR-0042** (Accepted, owner sign-off the same day),
#   which splits the two jobs: epics.md keeps the requirement registry, the
#   tracker set is the work ledger. The migration executes as Lane Z of Sprint 7,
#   and #321 closes by hand there. Recorded here rather than deleted because this
#   log is the project's memory of what was open when -- and because a finding
#   carried five times without resolution is worth showing resolved.
#
#   F8 -- ONE OPEN OWNER QUESTION carried out of the triage: the Round 7 proposal
#   of an ADR-0026 GATE-CLOSURE RULE (how a decision gate is recorded as closed).
#   It was deliberately left unwritten because it changes how this project makes
#   decisions, which is an owner call.
#   **RESOLVED 2026-08-18 by ADR-0043** (Accepted, owner sign-off): a gate-closing
#   ADR carries the gate's asks, each marked answered or deferred with a reason,
#   checked at the signature. Written as a proposal rather than put back to the
#   owner as a question, because the recommendation was strong and the owner had
#   said so plainly: if you recommend one, do not ask. Its one retroactive
#   application (ADR-0039's list) runs in Sprint 7 Lane Z step 7.
#
# NO story row and NO epic status changes this pass -- all 2026-08-15 work is
# decision artifacts plus backlog filing, and none of it hangs off an epics.md
# story row.
#
# SPRINT 6 CLOSED OUT (2026-08-14, `main` at f0beb8c):
# All lanes shipped in ONE batch/PR (#688), REBASE-merged -- the first under
# the ADR-0026 merge-method amendment (owner decision, same PR): 41 per-issue
# commits on main, agent-cleaned history (47 -> 41, mechanical fix-ups folded,
# review-round commits kept, tree byte-identical). Issues #664-#667 #686 #651
# #561 #668-#671 #673 #412 #491 #564 #565 #566 #674 #676 #682 closed by the
# merge's closing keywords (spot-verified). Six-role agentic review, every
# confirmed finding fixed pre-merge (delta-read as_of loss x2, ADR-0039
# race-safety pin, ?q[]= crash, segmented-control spec regression, doc
# drift). Gates at merge: 1855 tests/0 failures, Dialyzer zero, coverage
# 90.5%. Retrospective: sprint-6-retro-2026-08-14.md (close-out section
# filled in this pass). UX-DR remainder for Sprint 7: #414, #560 (+#672 as
# committed by OQ-2). OPEN OWNER ITEMS: v0.5.0 tag on 73affc5 AND v0.6.0 tag
# on f0beb8c -- both blocked from agent sessions (proxy allows only the
# designated branch; no release API tool). Prepared commands sit in PR #688
# and the retro. FR map updated: FR-37/38 shipped (agent-only, human view
# due next batch), FR-39/40 issue-ready (mechanism landed).
#
# SPRINT 6 ADOPTED (2026-08-12, `main` at 541fb89):
# sprint-plan-2026-08-12-sprint6.md, from the owner's stated expectation
# (finish the UI topics, solve the computed-numbers problem, ship some of the
# LLM agent's requests) with the three draft questions answered.
#   - OQ-1 OWNER: ADR-0039 is SIGNED OFF. Gate B3.2 is closed; the ADR flips to
#     Accepted and ADR-0032 becomes Superseded by it (its volatile memo becomes
#     the `:request` lifetime, the mechanism is not withdrawn). Lane C runs.
#   - OQ-2 AGENT: Lane A ships eleven E11 alignment/defect issues; #672 and
#     #414 stay out as new surfaces. Stated consequence: after Sprint 6 the UI
#     is ALIGNED but NOT FINISHED -- Sprint 7 is committed as the closer.
#   - OQ-3 AGENT: #572 stays out. It carries three unresolved design questions
#     of its own, its inflation/CPI half needs a source gated at B3.3, and
#     pairing it with Lane C puts two money-domain heavyweights in one batch.
# Lanes: Z close-out debt (the unpushed v0.5.0 tag, #682) - B agent requests
# (#664 first, then #665 #666 #667) - A UI (#651 #561 + #668-#673 + #412 #491
# #565 #566 #564) - C durable derived values (risk-tier) - M maintenance
# (#674 #676). No story rows change: Sprint 6 work hangs off no epic row, and
# section J still has no epic (finding F2 below).
#
# SPRINT 6 AMENDED (2026-08-14): Lane D added - #686 - and RE-SCOPED the same
# day, after the first diagnosis proved wrong. Recorded in full because the way
# it was wrong is the reusable lesson.
#
# As filed: "a hand-entered loss sale cannot book its tax refund", read from the
# changeset rule `taxes >= 0` plus `tax_refund` being absent from the form's
# type selector. Proposed fix (OQ-4 option A): teach the manual form to accept a
# negative tax and auto-split it into a companion `tax_refund` the way both PP
# importers do.
#
# What the owner reported: `tax_refund` is present AND in live use -- most of
# the existing bookings arrived through the PP import, and one was made by the
# very agent that reported the capability missing, weeks earlier. The ledger was
# never wrong. OQ-4 AND OPTION A ARE WITHDRAWN: the `taxes >= 0` rule stays,
# because it is what keeps ONE representation of a refund. The importers split a
# signed value only because PP's file format hands them one; manual entry has no
# such constraint, so building it would have added a second way to express a
# refund in order to fix a discoverability problem. Option B (sign-bearing taxes
# on buy/sell) was rejected before and stays rejected, for that same reason.
#
# The lane is now the affordance gap that actually caused the failure: (D1) the
# rejection is a bare Ecto message naming no alternative, (D2) `tax_refund` is
# missing from the direction enumeration in transactions.create, (D3)
# holdings.reconcile names set_balance as a last resort without ever naming
# tax_refund as the right answer, (D4) the type value appears once in all of
# docs/, inside ADR-0031. Consequence is data quality, not convenience: the
# surface steers agents toward set_balance, which the schema itself calls
# balance-looks-right-while-hiding-what-happened.
#
# NOT risk-tier, no ADR, no open owner question -- text, two schema
# descriptions, one error message, one doc section. The "sequence before Lane A,
# correctness defect" argument is DROPPED; the lane now runs with Lane B by
# affinity (both agent-surface work, one API/MCP review pass). Pinned by a test
# that the prose kind-lists stay in sync with Transaction.kinds/0, since the
# root cause is a hand-maintained list drifting from the schema.
#
# Process finding: the first pass verified the validation rule and the LiveView,
# then stopped -- it never read the MCP tool descriptions, never reproduced the
# error an agent receives, and never checked whether the capability was already
# in use. For a defect reported BY an agent, the agent-facing surface is the
# primary evidence. The reporter's own transcript carried the disproof.
#
# #686 hangs off epic tracker #470, so no story row changes.
#
# RECONCILED (2026-08-12, `main` at 541fb89):
# Verification basis: the merge commits on `main` (73affc5..541fb89), the open
# pull-request list (EMPTY), the open-issue list (46 open), the Actions runs on
# `main`, and the remote tag/release lists -- not labels and not this file.
#
# What landed since the Sprint 5 merge, none of it story-row work:
#   - 7d6b692 Sprint 5 bookkeeping close-out (ADR-0026 step 5).
#   - 7210a96 / dc393a7 owner feedback triage 2026-08-12 + product brief; the
#     confirmed items became thin issues #664-#677 (triage Round 4 is the index).
#   - 5c414ab identity gate B3.1: PRD, epics registry section J (FR-37..FR-48),
#     AGENTS.md -- the blanket "no advanced reports" rule is replaced by the
#     scope ladder (a)-(c) in scope, (d) backtesting gated; API/MCP coverage
#     became a TWO-WAY rule with a next-batch deadline for the human view.
#   - 096b250 / a3ecf6e AGENTS.md: closing keywords on PRs, rule vs. rationale.
#   - 14c18a0 architecture document re-validated against the 2026-08-12 base.
#   - 541fb89 ADR-0039 (durable derived values, gate B3.2) -- merged as
#     Proposed; owner signed off later the same day, so it is now ACCEPTED and
#     ADR-0032 is Superseded by it. See the Sprint 6 adoption block above.
#   - #677 (audit-journal rollout) closed as NOT PLANNED on 2026-08-12: the
#     rollout was already complete, so the asserted FR-43..FR-46 dependency does
#     not exist. Recorded in epics.md section J; nothing here was blocked by it.
#
# NO story row and NO epic status changes this pass -- every epics.md story
# still resolves to the same state it held at the Sprint 5 close-out, and all
# 2026-08-12 work was planning/decision artifacts plus backlog filing.
#
# THREE FINDINGS, recorded rather than silently carried:
#
#   F1 -- The Sprint 5 close-out below claims "annotated tag v0.5.0 on 73affc5
#   triggers the new Release workflow". It did not happen. `git ls-remote
#   --tags origin` carries exactly one tag (`before-agentic-run`) and the
#   repository has ZERO releases. ADR-0026 step 5 is therefore INCOMPLETE for
#   Sprint 5, and the first exercise of the #659 release automation has not run.
#   Action: create and push the annotated tag on 73affc5, or record why not.
#
#   F2 -- Section J (FR-37..FR-48, added 2026-08-12) hangs off NO epic row. The
#   Epic List still ends at E19. Two of the twelve are filed (#665 FR-37, #666
#   FR-38, both ship-now); the rest are gated (B3.2/B3.4/B3.6/B4.1/B4.2) or have
#   no issue. FR-37 is the direct successor to story 6.DX-4 (E6, closed), so the
#   agent-native family currently has no home in either epic structure. This
#   widens the standing two-parallel-structures finding (epics.md E1-E19 vs. the
#   GitHub tracker set #416-#420, #470, #398, #356) rather than being a new one.
#   The structural decision is still the owner's and still open; #321 is stale.
#
#   F3 -- CI on `main` is green through 14c18a0 (CI + Commit authorship + pages,
#   all success). The run on the head commit 541fb89 was still in_progress when
#   this file was written; Commit authorship on 541fb89 had already passed.
#
# CLOSE-OUT (2026-08-10, `main` at 73affc5 -- Sprint 5, PR #661):
# The whole sprint shipped as ONE batch/PR per the adoption decisions: Lane A
# tokens/contrast (#642 #643 #644 #648 #650 + spec cleanups), Lane B colour
# independence (#637 #645), Lane C loading affordances/opt-in motion
# (#638 #647 + the owner-decided cue/count-up/sunburst patterns), Lane D
# UX-DR11 tooltips (spec-driven), Lane E defect sweep (#634 #635 #636 #639
# #640 #641 #646 #649), Lane F CI/release engineering (#653 #654 #659). All
# twenty issues closed; #651 stays queued. Four new invariant meta-tests pin
# the fixed drift classes (theme-token parity, motion gates, ISO date inputs,
# native dialogs). The four-role agentic review (ADR-0026 step 3, incl. a
# live-server UAT with screenshots, uat-sprint5-2026-08-10/) caught and fixed
# the sprint's worst bug pre-merge -- morphdom stripping the native dialogs'
# open attribute -- plus two computed-style cascade regressions no DOM test
# could see. Retrospective: sprint-5-retro-2026-08-10.md (incl. two process
# gaps that had to be owner-prompted: the all-lanes doc audit and the
# screenshot refresh -- both now standing steps in the dev-agent contract).
# Docs screenshots + tour GIF regenerated from priv/demo (which gained an
# offline quotes seed; the strategies seed was repaired). NOTE: the batch ran
# on claude/sprint-5-alle-lanes-2siqq9, not the planned
# agent/claude/sprint-5-design-debt -- harness-fixed branch name, content
# unaffected. First automated release: annotated tag v0.5.0 on 73affc5 was to
# trigger the new Release workflow (issue #659; ADR-0026 step 5 amended in
# AGENTS.md) -- SEE FINDING F1 ABOVE: the tag was never pushed. No story rows
# change -- Sprint 5 work hangs off no epic row (the two-parallel-structures
# finding). E1 gains the fatal form-id warning and the test-failure artifact as
# shipped CI hardening (#653/#654).
#
# SPRINT 5 ADOPTED (2026-08-10, `main` at cf68260):
# The 2026-08-10 sequencing draft is adopted as sprint-plan-2026-08-10-sprint5.md
# with the owner's answers to its three open questions: ALL lanes in ONE batch
# on one epic branch (agent/claude/sprint-5-design-debt), ONE PR for the sprint
# (a lane splits into its own PR only if it turns out big -- decided up front,
# Sprint-4 retro item 1), and #572 stays out (E5 money-domain work, Sprint 6
# front-runner). Lane->issue mapping: A tokens/contrast (#642 #643 #644 #648
# #650 + spec cleanups), B colour independence UX-DR7 (#637 #645), C loading
# affordances/motion (#638 #647 + owner-decided skeleton/count-up patterns),
# D hint prose -> tooltips (spec-driven, no issue), E markup/i18n/nav defect
# sweep (#634 #635 #636 #639 #640 #641 #646 #649), F CI + release engineering
# (#659 release automation -- new, owner 2026-08-10: annotated vX.Y.Z tag in
# the ADR-0026 step-5 close-out + CI workflow on tag push creates the GitHub
# Release with generated notes, first tag v0.5.0 at the Sprint 5 merge --
# plus #653 #654). #651 stays queued. No story rows change -- Sprint 5 work
# hangs off no epic row (the two-parallel-structures finding).
#
# CLOSE-OUT (2026-08-10, `main` at 5167f27 -- Sprint 4 Lanes B+C, PRs #656/#657):
# Sprint 4 is complete. Lane B (#560, PR #656) made the income charts scroll
# horizontally on narrow viewports (UX-DR15 scroller + affordance). Lane C
# (#568, PR #657) delivered ADR-0034: Newton-primary XIRR with bracketed
# fallback (Excel-parity pinned), invested_capital / wealth_multiple / mwr in
# the period summary, the Wealth KPI row (two labeled numbers, n/a semantics,
# MWR label for sub-year windows), API + MCP parity, EN/DE docs. The ADR-0034
# "flow classifier" resolved as verification of the projection's existing
# per-kind `external` flag (ADR-0011 stays the single owner of booking
# semantics) -- relevant for #572, which reuses exactly those markers and is
# now unblocked. The agentic-review closing act (correctness + edge-case
# hunter) confirmed six findings, all fixed on the branches before merge.
# Both issues auto-closed with the squash-merges; merge CI verified green.
# Retrospective: sprint-4-retro-2026-08-10.md. Sprint 5 sequencing draft
# (from the confirmed design spec and the filed #634-#654):
# sprint-plan-2026-08-10-sprint5-draft.md. No story rows change -- #560/#568
# hang off no epic row (the two-parallel-structures finding).
#
# CLOSE-OUT (2026-08-06, `main` at 58ef873 -- Sprint 4 Lane A, PR #652):
# The design gate passed. The owner's merge IS the confirmation ADR-0026 step 4
# and the sprint plan require, so the living design-language spec
# (`planning-artifacts/design-language/`) is now the authority the E14/E11
# Sprint 5 stories are cut from, and the one a design-critic review holds every
# user-visible batch against (ADR-0038).
#
# What the batch produced beyond the spec itself:
#   - UX-DR1..UX-DR20 moved into the spec; `epics.md` links rather than defines
#     them. Six rules are new, from the live-surface survey and design critique.
#   - The IA covers all built routes; the frozen seven-surface scope is retired.
#   - Eleven drift families each carry one canonical pattern plus the real list
#     of deviating call sites, so a Sprint 5 story inherits a work list instead
#     of repeating the survey.
#   - 21 issues filed (#634-#654). Most are defects the OLD spec had recorded as
#     shipped -- buy/sell markers, native <dialog>, reduced motion as opt-in,
#     the chart-as-table census, the data-quality link.
#
# Lanes B (#560) and C (#568) are NOT part of this close-out; Sprint 4 remains
# open until they land or are deferred.
#
# Two follow-ups are downstream work, not open design questions: categorical
# colour is its own decision gate (it touches user-set data), and the Overview
# data-quality line has no pre-filtered URL to link to (#651) -- the 2026-07-12
# decision turned out to be not merely unbuilt but currently impossible.
#
# CLOSE-OUT (2026-08-04, `main` at 1903913 -- Sprint 3, PR #631):
# The whole sprint shipped as one bundle on owner instruction: #569 (ADR-0033
# P&L decomposition + settlement backfill), #620 (FIFO tranches at the sale),
# #577 (cross-portfolio performance walk), #563 (period picker), #606
# (microcopy voice sweep), #619 (ADR-0035 one pricing pass per read). All six
# closed. Four ADRs accepted: 0034 (money-weighted metrics design note for
# #568 -- NOT implemented, issue stays open), 0035, 0036 (risk-tier rides the
# batch, amends 0026), 0037 (Phoenix 1.8 / LiveView 1.x security upgrade).
# None of these issues hangs off an epic row below -- E5/E11 analytics and UX
# work is tracked by GitHub issue, not by story rows (the two-parallel-
# structures finding), so no story row changes. E11 gains #606 as shipped.
# Dependency posture: 15 advisories / 5 HIGH on main -> 2 / 0 HIGH.
#
# CLOSE-OUT (2026-08-01, `main` at ba6a046):
# Sprint 2 Lane A merged same-day as the owner-approved combined PR #629:
# #406 (truthful "no price" warning, detail/totals reconciled on the same
# price-resolution semantics) and #570 (negative holdings flagged per depot
# and total, linked to the security's transactions). Tracker #398 closed with
# them -- its second sub-issue (cash-to-currency) had already shipped. UAT
# walkthrough artifacts: uat-sprint2-lane-a-2026-08-01/. ADR-0033 accepted
# (58c90f5), unblocking #569 + #620 for Sprint 3. The commit-authorship gate
# ran GREEN on ba6a046 -- the first squash-merge after fix ee51260; the
# red-gate standing finding is resolved. #406/#570/#398 hang off no epic row
# (the two-parallel-structures finding, see
# epic-structure-reconciliation-2026-07-31.md), so no story rows change.
#
# RECONCILED (2026-07-31, `main` at 02dde3b):
# Sprint 1 merged in full -- all three lanes, not just lane A. Lane A shipped
# E19 stories 19.2-19.6 (#621-#625), lane B shipped #607/#609, lane C shipped
# #562 with ADR-0032. This file had been left at "awaiting review" while the
# work was already on `main`; the story rows below now follow the merge, not
# the branch. Verified against the merge commit, the closed-issue list and the
# (empty) open-PR list.

development_status:
  # E1 -- open: #314 (coverage ratchet + Credo thresholds).
  # Shipped since: #653/#654 CI hardening (Sprint 5). #682 (capturable
  # intermittent local test-failure bursts) is new and unattached.
  epic-1: in-progress
  epic-1-retrospective: optional

  # E2 -- open: #354 (pg_dump backup/restore, rescoped); #353 audit journal done.
  # #677 confirmed the rollout complete and closed as not_planned 2026-08-12.
  epic-2: in-progress
  epic-2-retrospective: optional

  # E3 -- open: #328 (merge/rename/delete accounts, needs-uat); #608 (merge and
  # repair securities, the #328 analogue) is unattached
  epic-3: in-progress
  epic-3-retrospective: optional

  # E4 -- open: #333 (PP XML import) -- GATED, needs ADR + AGENTS.md amendment
  epic-4: backlog
  epic-4-retrospective: optional

  # E5 -- #316/#331/#318/#329/#335/#334 all closed
  epic-5: done
  epic-5-retrospective: optional

  # E6 -- DX batch #581-#585 all closed; verified in mcp-server/src/tools.ts.
  # See finding F2: FR-37 (#665) and FR-38 (#666) are the agent-native successor
  # work on this same surface and hang off no epic row.
  epic-6: done
  epic-6-retrospective: optional

  # E7 -- ADR-0023 display-only hints shipped; ranked both-direction cash
  # guidance open (no issue). The rebalancing digest (B3.5) is gated.
  epic-7: in-progress
  epic-7-retrospective: optional

  # E8 -- read-only sync, GATED (#320). Unchanged by the scope ladder: the
  # identity gate kept acquisition beyond quotes/FX gated at B3.3.
  epic-8: backlog
  epic-8-retrospective: optional

  # E9 -- product types, discovery-first (#330, #340)
  epic-9: backlog
  epic-9-retrospective: optional

  # E10 -- planning & simulation. #332 (what-if) still gated, now on level-(d)
  # grounds. FR-9 benchmark comparison was UNGATED 2026-08-12 by the scope
  # ladder (level (b)); its filed issue is #572, which the Sprint 5 plan called
  # E5 work -- the Epic List assigns FR-9 to E10.
  # First shipped item 2026-09-15 (Sprint 11 Lane B, PR #810, main at
  # c2164f54): #572 closed -- ADR-0046 executed as signed, two benchmark
  # kinds, both comparisons, API and MCP first and the Wealth page in the
  # same batch. Deferred by ADR-0046 section 6 with reasons: a dated CPI
  # table, the after-tax dimension, a stored per-view default. FR-26 stays
  # discovery-first; #332 stays gated.
  epic-10: in-progress
  epic-10-retrospective: optional

  # E11 -- open: #356 tracker + the 2026-09-12 review's remainder (#806 #807
  # #808 #809, Sprint 13 candidates declared by D-3) and the Sprint 12 closing
  # act's three findings (#815 #816 #817). The August list this row used to
  # carry is closed.
  # First alignment pass: Sprints 7-8 (#414 #672 #701-#704 #706 #707, then
  # #717-#721 #723 #729 #730), which added UX-DR25 and UX-DR26 to the spec.
  # Second alignment pass shipped 2026-09-15 (Sprint 12, PR #813, main at
  # 5ea57ea3): twenty-two issues closed, #784-#805 contiguous, across Lanes F,
  # N, S, K, P, T and D; UX-DR27 (phone list rows) and the UX-DR2 amendment
  # for the Overview strip came with it, and DESIGN.md names all eight owner
  # picks (C1-A C2-B C3-A C4-B C5-A C6-C C7-A C8-A).
  # Stays in-progress: the tracker's remainder is declared Sprint 13 work.
  epic-11: in-progress
  epic-11-retrospective: optional

  # E12 -- #313 closed; bilingual docs site live (ADR-0014)
  epic-12: done
  epic-12-retrospective: optional

  epic-13: done
  epic-13-retrospective: optional

  epic-14: done
  epic-14-retrospective: optional

  epic-15: done
  epic-15-retrospective: optional

  # E16 -- ADR-0027 shipped (snapshots_live.ex, plan versions)
  epic-16: done
  epic-16-retrospective: optional

  # E17 -- #588-#591 closed and merged; review debt (#609) cleared in Sprint 1;
  # tracker #338 closed 2026-07-31
  epic-17: done
  # combined E17-E19 retrospective 2026-07-31 (epic-17-18-19-retro-2026-07-31.md)
  epic-17-retrospective: done

  # E18 -- #600-#602/#605 closed and merged; review debt (#607, #609) cleared in
  # Sprint 1; tracker #603 closed 2026-07-31. #664 (verify a PP re-import
  # preserves classification/targets/notes/attributes) re-tests story 18.2's
  # guarantee and is open, risk-tier attention -- it verifies, it does not reopen.
  epic-18: done
  # combined E17-E19 retrospective 2026-07-31 (epic-17-18-19-retro-2026-07-31.md)
  epic-18-retrospective: done

  # E19 -- gate #612 signed off 2026-07-25; ADR-0031 Accepted; 19.2-19.6
  # (#621-#625) merged to main in 02dde3b and closed. Epic stays in-progress:
  # 19.7 is deferred behind its own decision gate, by design. #667 (staleness
  # warning + allowance-order entry) is open follow-up on the shipped surface.
  epic-19: in-progress
  # combined E17-E19 retrospective 2026-07-31 (epic-17-18-19-retro-2026-07-31.md)
  epic-19-retrospective: done

  # E20 -- Security knowledge log (ADR-0044, signed 2026-09-03). Tracker #747
  # with #748-#752 as the work ledger; Sprint 9 Lane A, batch branch opened
  # 2026-09-03. The row exists so the close-out reconciles against it instead
  # of inventing one (Sprint 9 plan, Lane Z).
  # Closed out 2026-09-03 (PR #754, main at 6de32ff): every ADR-0044 scope
  # item shipped; retrospective sprint-9-retro-2026-09-03.md.
  epic-20: done
  epic-20-retrospective: done

  # E21 -- Security hardening (ADR-0045 signed 2026-09-05; triage
  # security-review-triage-2026-09-05.md adopted the same day). Tracker #757
  # with #758-#772 as the work ledger; Sprint 10, batch branch opened
  # 2026-09-05. #382 (CSP) and #772 (Bandit) are the next batch's lanes.
  # Closed out 2026-09-06 (PR #773, main at b701613a): every lane of the
  # triage's Part 3 that the batch claimed shipped; #382 and #772 are the
  # next batch by the triage's own ruling. Retrospective
  # sprint-10-retro-2026-09-06.md; the surface check filed #776.
  # Remainder shipped 2026-09-15 (Sprint 11 Lanes D, C, W; PR #810, main at
  # c2164f54): #772 Bandit with cowlib's advisories gone and every Action
  # pinned to a SHA, #382 the per-request CSP nonce and the HTTPS posture
  # (Sobelow with no ignore), #776 the limit surface complete but for the
  # journal read's own parser (#811). Nothing in the family stays open.
  epic-21: done
  epic-21-retrospective: done
